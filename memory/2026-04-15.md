# 2026-04-15

## WriteHabit App Development

### Font System Fixes
- Fixed font changes not being applied in RichTextEditor
- Added `.id()` modifier to force view recreation when font/size/lineSpacing change
- Updated `makeUIView` in RichTextEditor to apply current font to existing attributed text on recreation
- This ensures font changes take effect immediately without losing text content

### UI Structure Changes
- **Settings → Appearance**: Now only has Theme and Accent Color (removed Font option)
- **Editor → Format**: Has Formatting, Alignment, Font, Size, and Line Spacing

### Fixed Compiler Issues
- Fixed `ForEach` duplicate ID warning in KeywordArchiveView calendar weekday headers
- Changed from `ForEach(calendar.shortWeekdaySymbols, id: \.self)` to using enumerated indices
- Fixed emoji encoding issue by removing checkmark emojis from print statements
- Fixed "Invalid redeclaration of 'themeManager'" by removing duplicate in GrammarTextEditor.swift

### Current Status
- Font changes should now work properly in the editor
- Build succeeds after all fixes
- Ready for testing font functionality
## 2026-04-15 - DailyWrite Font Issue Debugging

Session with Jiyun fixing font persistence issues in the DailyWrite app.

### Issues Identified
1. **Saved/draft essays not displaying with saved fonts** - When viewing saved essays, fonts weren't being applied
2. **Fonts not applying in editor when writing** - Font changes in Format menu weren't updating the text

### Root Causes
1. **Editor not restoring saved font when opening existing essays** - The `RichTextEditor` was using current FontManager settings instead of the essay's saved font
2. **Attributed text encoding** - The attributed content was being saved, but font settings weren't being restored to FontManager when reopening

### Fixes Applied
- Modified `SimpleWritingEditorView.init()` to restore `fontName`, `fontSize`, and `lineSpacing` from essay to FontManager when opening existing essays/drafts
- Added fallback to system font if saved font can't be loaded
- Fixed debug logging syntax errors in `KeywordArchiveView.swift`

### Still Need to Test
- Verify font changes apply immediately in editor
- Test saved essays display with correct font/size/line spacing
- Verify Korean translations appear in Format menu

### Build Issues Encountered
- Linker error from duplicate files in `old_swift_files/` - resolved by moving directory
- Firebase framework corruption after package cache reset - resolved by cleaning SPM cache
- Syntax errors from leftover code - resolved by cleaning up duplicate function code

### Files Modified
- `/Users/leia/.openclaw/workspace/DailyWrite/DailyWrite/Views/SimpleWritingEditorView.swift` - Restored font settings from essay in init
- `/Users/leia/.openclaw/workspace/DailyWrite/DailyWrite/Views/KeywordArchiveView.swift` - Fixed debug logging syntax

## 2026-04-15 - Daily Notes

### Korean Font Fixes (Completed)
- Found correct PostScript names in font files: KoPubWorldBatangPM, KoPubWorldDotumPM, Pretendard-Regular, RIDIBatang
- Updated FontManager with correct PostScript name fallbacks
- Fixed Info.plist to only list Regular font variants (Bold didn't exist)
- Added font re-application in KeywordArchiveView for saved essays
- Fixed font change to apply to selected text/current paragraph, not just new text
- New essays now save and display with correct Korean fonts
- Old essays (without font data) can be re-saved to get font support

### Like Button Bug (In Progress)
- Issue: Clicking essay in feed triggers like button unexpectedly
- Likely a SwiftUI hit-testing/tap propagation issue
- Added contentShape and allowsHitTesting modifiers to isolate button taps
- Need to test if the fix works

### Remaining Tasks
- Verify like button fix works in FeedView
- Test font changes apply to selected text properly
- General app stability testing


## 2026-04-15 - FeedView 4-Tab Implementation

### New Tab Structure (Completed)
Implemented new 4-tab structure for FeedView:
1. **팔로잉** (Following) - All/Friends Only sort
2. **오늘** (Today) - Today's essays only
3. **최신** (Recent) - Date/Likes/Trending sort
4. **주제별** (By Topic) - Past keywords with sort

### Sort Functionality
- Added sort dropdown UI for Following and Recent tabs
- Created sort enums: `FollowingSort`, `RecentSort`, `TopicSort`
- Updated `loadEssays()` function to handle new tab structure
- Added Korean translations for sort labels

### Friend-Follow Integration
- Modified `acceptFriendRequest()` to automatically add both users to each other's `following` array
- Modified `removeFriend()` to automatically remove from `following` array
- Now friends = mutual following is guaranteed

### Topic Cards (By Topic Tab)
- Created `TopicGroup` struct to represent a keyword with essays
- Implemented `TopicCardsView` - grid of keyword cards with emoji, name, essay count
- Implemented `TopicDetailView` - shows essays for selected topic with "Write" button
- Added "과거 주제" (Past Topic) indicator in editor when writing on past topics
- Writing on past topics doesn't affect streaks (just UI indicator)

### UI/UX Improvements
- Topic cards show: emoji, keyword name, essay count
- Topic sort: 키워드 (A-Z), 날짜, 인기
- Past topic writing: Opens editor with keyword pre-filled, shows orange "과거 주제" label
- Added Korean translations for new strings: "과거 주제", "작성 글", "전체", "날짜순", "인기순"

### Files Modified
- `/Users/leia/.openclaw/workspace/DailyWrite/DailyWrite/Views/FeedView.swift` - New tab structure, topic cards, sort UI
- `/Users/leia/.openclaw/workspace/DailyWrite/DailyWrite/Services/FriendsService.swift` - Auto-follow on friend add/remove
- `/Users/leia/.openclaw/workspace/DailyWrite/DailyWrite/Views/SimpleWritingEditorView.swift` - Past topic indicator
- `/Users/leia/.openclaw/workspace/DailyWrite/DailyWrite/Resources/ko.lproj/Localizable.strings` - Korean translations
- `/Users/leia/.openclaw/workspace/DailyWrite/DailyWrite/Resources/en.lproj/Localizable.strings` - English translations

### Next Steps
- Test topic cards display correctly
- Verify sort functionality works
- Test writing on past topics
- Check friend-follow integration

## 2026-04-15 - DailyWrite App Development

**Morning Session (09:30-10:15)**

### Keyboard Performance Optimization
- Fixed keyboard lag issue when first tapping RichTextEditor
- Root cause: Heavy synchronous work in `makeUIView` blocking main thread
- Solution: Refactored to apply font attributes asynchronously using `DispatchQueue.global(qos: .userInitiated)`
- Added keyboard warm-up hack using hidden text field in `SimpleWritingEditorView`

### Title Removal and Restoration
- Initially removed title field from editor (user thought essays don't need titles)
- Removed title display from EssayCard, TopicEssayRow, AllEssaysView, KeywordArchiveView, ProfileView
- User decided to bring titles back - "too much work"
- Restored title field in SimpleWritingEditorView
- Updated save/publish calls to pass `title` instead of empty string
- Views still need title restoration (paused to ask which ones)

### One Essay Per Keyword Rule
- Implemented check: users can only publish one essay per keyword
- Multiple drafts allowed (auto-save continues to work)
- Added `getPublishedEssayForKeyword()` and `hasPublishedEssayForKeyword()` in FirebaseService
- Shows alert "Already Written" → "Edit Existing" or "Cancel" when trying to publish duplicate
- Added Korean localization: "이미 작성됨", "기존 글 수정"
- Fixed missing FirebaseAuth import error

### Essay Detail View Titles
- Removed navigation title from EssayDetailView ("Essay" → no title)
- User requested removal for cleaner look when tapping essay cards

### Files Modified Today
- `/DailyWrite/DailyWrite/Views/RichTextEditor.swift` - Async font formatting
- `/DailyWrite/DailyWrite/Views/SimpleWritingEditorView.swift` - Title field, keyboard warm-up, publish check
- `/DailyWrite/DailyWrite/Services/FirebaseService.swift` - Essay per keyword check functions
- `/DailyWrite/DailyWrite/Localization.swift` - Korean translations
- `/DailyWrite/DailyWrite/Views/FeedView.swift` - Removed detail view navigation title
- `/DailyWrite/DailyWrite/Views/KeywordArchiveView.swift` - Removed detail view navigation title

### Pending Decisions
- Which views should display titles? (Feed cards, archive lists, profile)



## 2026-04-15 - DailyWrite App Development (11:00-11:45)

### Nanum Font Fix (Completed)
- Discovered actual PostScript names in TTF files: **"NanumMyeongjo"**, **"NanumGothic"** (no "Regular" suffix)
- Fixed FontManager.swift to use correct PostScript names
- Fonts now working correctly

### UI Improvements (Completed)

**1. Keyboard Dismissal**
- Added tap gesture to dismiss keyboard in SimpleWritingEditorView
- Screen tap now dismisses keyboard (in addition to existing swipe down)

**2. Text Alignment Persistence**
- Fixed alignment not working properly
- Fixed alignment resetting when changing font/line spacing
- Fixed alignment not persisting to new lines (줄바꿈)
- Alignment now saved and restored with essay

**3. Draft → Publish Fix**
- Fixed bug: `publishDraft(essayId:)` missing `visibility` parameter
- Changed to `publishDraft(essayId:visibility:)`
- Drafts can now be published properly

**4. All Essays View**
- Removed 임시저장 (Drafts) filter from 글보관함 (All Essays)
- Now only shows published essays
- Drafts accessible via Profile → 임시저장

### Files Modified
- `/DailyWrite/DailyWrite/Utils/FontManager.swift` - Nanum font PostScript names
- `/DailyWrite/DailyWrite/Views/SimpleWritingEditorView.swift` - Keyboard tap, publish fix
- `/DailyWrite/DailyWrite/Views/RichTextEditor.swift` - Alignment persistence fixes
- `/DailyWrite/DailyWrite/Views/AllEssaysView.swift` - Removed drafts filter
- `/DailyWrite/DailyWrite/Services/FirebaseService.swift` - publishDraft visibility parameter



## 2026-04-15 - DailyWrite Comment System Implementation (15:00-15:35)

### Comment Features Added
- Implemented full comment system with add/view/delete/like functionality
- Fixed comments not displaying (removed Firestore composite index requirement by sorting manually in Swift)
- Fixed comment author names (now uses app's username/nickname from Firestore users collection)
- Added comment delete functionality (only comment author can delete)
- Added comment likes (heart button with count)

### Reply Functionality
- Added reply (re-comment) feature for nested comments
- Shows reply count on parent comment
- Pre-fills "@username " when replying
- Shows "Replying to [username]..." indicator with cancel option
- Flat reply structure with @mention reference

### Notification System (Partial)
- Created NotificationsView for in-app notifications list
- Added notification bell with unread badge to ProfileView
- Real-time listener updates badge count
- Notifications saved to Firestore when comments/replies posted

**Note:** Push notifications require Firebase Cloud Functions (server-side) deployment. In-app notifications work correctly.

### Feed Following Fix
- Fixed Following tab not showing friends' essays
- Updated query to properly fetch public essays from all following + friends-only essays from actual friends
- Separate queries avoid Firestore composite index requirements

### Cloud Functions Setup
- Created Cloud Functions code for push notifications
- Functions: sendPushNotification, notifyOnComment, notifyOnLike
- Deployment requires Firebase CLI (`firebase deploy --only functions`)

### Files Modified
- `/DailyWrite/DailyWrite/Views/KeywordArchiveView.swift` - CommentsSectionView with delete/like
- `/DailyWrite/DailyWrite/Models/Essay.swift` - Comment model with likesCount/likedBy
- `/DailyWrite/DailyWrite/Services/FirebaseService.swift` - Comment queries, delete/like functions
- `/DailyWrite/DailyWrite/Services/NotificationService.swift` - Notification sending functions
- `/DailyWrite/DailyWrite/Views/ProfileView.swift` - Notification bell with badge
- `/DailyWrite/DailyWrite/Views/NotificationsView.swift` - New file (in-app notifications)
- `/DailyWrite/functions/index.js` - Cloud Functions for push notifications

### Remaining Issues
- NotificationsView.swift needs to be manually added to Xcode project
- Push notifications require Cloud Functions deployment
- Following feed needs testing after query fix


## 2026-04-15 - Profile Photo Implementation (15:50-16:00)

### Profile Photo Upload from iPhone Photos
- Created `ProfileImagePicker.swift` component using `PhotosUI` framework
- Implemented image picker with `PhotosPicker` for native iPhone Photos app access
- Created `AvatarView.swift` component for displaying profile photos (supports data URLs)
- Modified `EditProfileView.swift` to include profile image picker section

### Base64 Storage Approach
- Initially planned Firebase Storage but requires additional package dependency
- Switched to base64 string storage in Firestore user document (simpler, no extra dependencies)
- Images compressed to JPEG (30% quality) then converted to base64
- `AvatarView` handles both data URLs (`data:image/jpeg;base64,...`) and regular URLs
- `ProfileImageUploader` class manages upload/delete operations

### Files Created/Modified
- `/DailyWrite/DailyWrite/Views/Components/ProfileImagePicker.swift` - Image picker component
- `/DailyWrite/DailyWrite/Views/Components/AvatarView.swift` - Avatar display component
- `/DailyWrite/DailyWrite/Views/EditProfileView.swift` - Added photo picker section
- `/DailyWrite/DailyWrite/Services/FirebaseService.swift` - Added `updateUserProfilePhoto()` function

### UI Features
- Circular avatar with "Change Photo" button
- Loading indicator during upload
- Delete old image automatically when uploading new one
- Avatar displays in comments section (AsyncImage with fallback)

### Next Steps
- Test profile photo upload from iPhone Photos
- Verify avatars display correctly in comments
- Consider Firebase Storage migration for production (base64 increases size ~33%)


## 2026-04-15 - Profile Photo Debugging Session (16:30-16:53)

### Issue
Profile photo not saving/displaying after user taps "Save Changes" in EditProfileView.

### Investigation Steps
1. **Initial Problem**: `saveProfile()` not executing - no console output
2. **Root Cause**: Confirmation alert wasn't triggering `saveProfile()`
3. **Fix Applied**: Removed confirmation alert, now calls `saveProfile()` directly when "Save Changes" tapped

### Second Issue: Photo Not Uploading
1. **Debug Findings**: Console showed `newUrl: nil` after selecting photo
2. **File Not Compiled**: `ProfileImagePicker.swift` wasn't being compiled into app
   - Changes to file weren't reflected in running app
   - Confirmed by adding intentional compile error which didn't trigger
3. **Fix**: User had to check Target Membership in Xcode File Inspector
   - Made sure "DailyWrite" target was checked
   - After this, file compiled and picker rendered

### Current Status
- ProfileImagePicker now renders (seeing `* ProfileImagePicker RENDERING *` in console)
- But `PhotosPicker` with `onChange` wasn't firing when selecting photo
- User tried to save without photo being uploaded (hence `newUrl: nil`)

### Attempted Solutions
1. **PhotosPicker with onChange** - Not firing (iOS version issue?)
2. **PHPickerViewController** - Same database permission error
3. **UIImagePickerController** - Code written but not being compiled

### Known Issues
- Simulator showing `process may not map database` errors for photo library
- File changes require explicit clean build + target membership verification
- Still need working photo picker implementation

### Next Steps
- Get UIImagePickerController version to compile and test
- Consider testing on physical device (simulator has photo library issues)
- Verify `onImageSelected` closure is properly connected to `uploadProfileImage`

### Key Lesson
Always verify Xcode Target Membership when files exist but changes don't appear in app. Clean build folder (Shift+Cmd+K) between changes.
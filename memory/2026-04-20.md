# 2026-04-20 - DailyWrite App Updates

## Calendar Streak Enhancement
- Added `signupDate` to `DayDot` view
- Days before user signup now show **gray** instead of burnt orange (missed)
- Prevents new users from seeing "missed" days before they joined

## By Topic Section Fix
- Fixed: Keywords weren't being archived to Firebase
- Added `archiveKeyword()` call when daily keyword loads
- Created `seedKeywordsFromLaunch()` function
- Generates deterministic keywords from April 14, 2026 → today
- All users share same global topic history
- Added "Load All Topics" button for backfill

## Keyword Emoji Fix  
- Fixed: All keywords showing same emoji (✨)
- Added hash-based fallback for unmatched keywords
- Same keyword = same emoji (consistent across sessions)

## Files Modified
- `DailyWrite/DailyWrite/Views/DailyPromptView.swift` - Calendar with signup date
- `DailyWrite/DailyWrite/Views/FeedView.swift` - By Topic error handling + backfill button
- `DailyWrite/DailyWrite/Services/KeywordsService.swift` - Seed and backfill functions
- `DailyWrite/DailyWrite/Services/KeywordEmojiService.swift` - Hash-based emoji fallback

## Beta Status
- TestFlight approved, in beta testing
- Collecting user feedback

## Evening Session - Keyword Seeding & Emojis

### Fixed Swift Runtime Errors
- **Arithmetic overflow** in `seedKeywordsFromLaunch()` - Fixed modulo operations with bounds checking
- **Hash overflow** in `randomEmoji(for:)` - Changed to overflow-safe operators (`&+`, `&<<`, `&-`)

### Successful Topic Seeding
- Seeded **31 new keywords** from March 19 → April 19, 2026
- Updated **1 existing** with Korean translation
- Keywords now appear in By Topic view

### Meaningful Emojis
- Added comprehensive English/Korean keyword → emoji mappings
- Examples: Spring→🌸, Journey→🗺️, Truth→💎, Memory→🎞️
- Falls back to 200+ emoji pool for unmatched keywords

### Korean Display for Past Topics
- `SimpleWritingEditorView` now accepts `displayKeyword` parameter
- Shows Korean keyword in UI, stores English in database
- Fixed: Past topic writing view was showing English instead of Korean

### Auth Localization Fix
- Added missing strings to `Localization.swift`:
  - "Already have an account? Sign In" → "이미 계정이 있으신가요? 로그인"
  - "Don't have an account? Sign Up" → "계정이 없으신가요? 회원가입"

### Files Modified
- `DailyWrite/DailyWrite/Services/KeywordsService.swift` - Overflow-safe index calculation
- `DailyWrite/DailyWrite/Services/KeywordEmojiService.swift` - Added `emojiMapEnglish` and meaningful mappings
- `DailyWrite/DailyWrite/Views/SimpleWritingEditorView.swift` - Added `displayKeyword` parameter for Korean UI
- `DailyWrite/DailyWrite/Views/TopicDetailView.swift` - Pass Korean keyword as displayKeyword
- `DailyWrite/DailyWrite/Localization.swift` - Added auth toggle translations

### Pending
- User feedback on Auth UI shifting (suggested tab-style toggle)

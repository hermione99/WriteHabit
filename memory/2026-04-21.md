# Memory - April 21, 2026

## DailyWrite App Development

### Card-Based Flip Design Implementation
Working on a major redesign of the DailyPromptView (Write tab) to use card-based flip interactions.

**Design Evolution:**
- Started with 3-column grid, pivoted to horizontal carousel, then settled on vertical scroll with full-width cards
- Final design: Vintage ticket-style cards with flip animation
- Color palette changed from earth tones to vibrant tricolor: Pink (#C87E96) for missed, Teal (#C9414E) for done, Chartreuse (#D6D385) for empty

**Current Status:**
- Created FlipCardView.swift with 3D rotation animation
- Implemented ticket shape with perforated edges
- Front shows date + status (TODAY/MISSED/DONE stamp)
- Back shows keyword in coffee label style with word count, likes, comments stats
- Vertical scroll through 31 days (past 30 + today)

**Files Modified:**
- DailyPromptView.swift - Card grid layout
- FlipCardView.swift - Ticket-style flip card component
- KeywordsService.swift - Added getKeywordForDate() helper

**Next Steps:**
- Test build and fix any remaining compile errors
- Move streak/dot calendar to ProfileView
- Polish animations and interactions


---

## Later Session - Monthly Card Deck with Coffee Label Aesthetic

**Palette Update:**
User provided a new sophisticated color palette from Jenny Henderson Studio:
- Navy (#374375) - Primary dark
- Cloud (#FFFCF5) - Off-white/cream
- Periwinkle (#BABDE2) - Soft blue-lavender  
- Maroon (#895159) - Deep reddish-brown
- Peach (#DFAAA1) - Soft pink-beige

**Design Pivot:**
Pivoted to coffee label aesthetic inspired by coffee packaging - clean, elegant typography with:
- Thin horizontal dividers (0.5pt lines)
- Grid layout for details (MONTH / STATUS / WRITING)
- Large day numbers with small uppercase date labels
- Circular status stamps (checkmark, TODAY, LOCKED)
- Sharp 4pt corners (more refined than rounded cards)
- Subtle shadows for depth

**Architecture Changes:**
- Complete rewrite of DailyPromptView.swift
- Monthly deck with horizontal TabView for month swiping
- Vertical ScrollView for days 1-31 within each month
- Future days locked (not flippable, keyword secret)
- Auto-scroll to today's card on load
- Back card shows keyword in quotes with write button or stats

**Color Mapping Updated:**
- Navy (#374375) - Done/Written cards + accents
- Maroon (#895159) - Missed/Past empty cards
- Cloud (#FFFCF5) - Empty/Future cards + background + back side

**Fixes Applied:**
- Fixed duplicate Color.init(hex:) extension in FlipCardView.swift
- Added getKeywordForDate(date:language:) to KeywordsService.swift
- Updated DailyPromptView to use KeywordsService instead of KeywordGenerator for per-date keywords

**Time:** 04:44 KST (April 21)

## DailyWrite Color Palette Update (05:32)

Updated card color scheme:
- **Burnt Nectar** (#C2441C) - Future cards
- **Moss Shadow** (#AA542B) - Done/completed cards
- **Ruby Leaf** (#852E47) - Missed days
- **Cream** (#F5F0E8) - Today's card (empty/active state)

Added approved stamp design with stars around circle edge and diagonal stripes for DONE status.

Fixed Shape modifier chain - `.fill()` must come before `.frame()` when using custom Shape.

Added `onEssayTap` closure to MonthCardView for navigating to essay detail when tapping back of written cards.

---

## Memory Flush - April 21, 2026 06:49 AM

### DailyWrite Keyword Stability Fix

**Problem:** Keywords were changing position every time the app launched because `String.hashValue` is not stable across app launches.

**Solution:** Replaced unstable hash with stable date-based calculation:
```swift
// Old (unstable):
let poolIndex = abs(dateString.hashValue) % 100

// New (stable):
let components = calendar.dateComponents([.year, .month, .day], from: date)
let poolIndex = ((components.year! * 10000) + (components.month! * 100) + components.day!) % 100
```

**Files Modified:** DailyPromptView.swift - `generateMonthCards()` function

### DailyWrite Card Back Design

User preference confirmed: Back of cards should have **cream background** with **colored text** matching the front card color (not cream text on colored background).

This creates a nice visual effect where the card's state color (Indigo/Moss/Ruby/Burnt) carries through to the back via the text color.

---
# Memory - April 21, 2026

## DailyWrite App Development

### Card-Based Flip Design Implementation
Working on a major redesign of the DailyPromptView (Write tab) to use card-based flip interactions.

**Design Evolution:**
- Started with 3-column grid, pivoted to horizontal carousel, then settled on vertical scroll with full-width cards
- Final design: Vintage ticket-style cards with flip animation
- Color palette changed from earth tones to vibrant tricolor: Pink (#C87E96) for missed, Teal (#C9414E) for done, Chartreuse (#D6D385) for empty

**Current Status:**
- Created FlipCardView.swift with 3D rotation animation
- Implemented ticket shape with perforated edges
- Front shows date + status (TODAY/MISSED/DONE stamp)
- Back shows keyword in coffee label style with word count, likes, comments stats
- Vertical scroll through 31 days (past 30 + today)

**Files Modified:**
- DailyPromptView.swift - Card grid layout
- FlipCardView.swift - Ticket-style flip card component
- KeywordsService.swift - Added getKeywordForDate() helper

**Next Steps:**
- Test build and fix any remaining compile errors
- Move streak/dot calendar to ProfileView
- Polish animations and interactions


---

## Later Session - Monthly Card Deck with Coffee Label Aesthetic

**Palette Update:**
User provided a new sophisticated color palette from Jenny Henderson Studio:
- Navy (#374375) - Primary dark
- Cloud (#FFFCF5) - Off-white/cream
- Periwinkle (#BABDE2) - Soft blue-lavender  
- Maroon (#895159) - Deep reddish-brown
- Peach (#DFAAA1) - Soft pink-beige

**Design Pivot:**
Pivoted to coffee label aesthetic inspired by coffee packaging - clean, elegant typography with:
- Thin horizontal dividers (0.5pt lines)
- Grid layout for details (MONTH / STATUS / WRITING)
- Large day numbers with small uppercase date labels
- Circular status stamps (checkmark, TODAY, LOCKED)
- Sharp 4pt corners (more refined than rounded cards)
- Subtle shadows for depth

**Architecture Changes:**
- Complete rewrite of DailyPromptView.swift
- Monthly deck with horizontal TabView for month swiping
- Vertical ScrollView for days 1-31 within each month
- Future days locked (not flippable, keyword secret)
- Auto-scroll to today's card on load
- Back card shows keyword in quotes with write button or stats

**Color Mapping Updated:**
- Navy (#374375) - Done/Written cards + accents
- Maroon (#895159) - Missed/Past empty cards
- Cloud (#FFFCF5) - Empty/Future cards + background + back side

**Fixes Applied:**
- Fixed duplicate Color.init(hex:) extension in FlipCardView.swift
- Added getKeywordForDate(date:language:) to KeywordsService.swift
- Updated DailyPromptView to use KeywordsService instead of KeywordGenerator for per-date keywords

**Time:** 04:44 KST (April 21)

## DailyWrite Color Palette Update (05:32)

Updated card color scheme:
- **Burnt Nectar** (#C2441C) - Future cards
- **Moss Shadow** (#AA542B) - Done/completed cards
- **Ruby Leaf** (#852E47) - Missed days
- **Cream** (#F5F0E8) - Today's card (empty/active state)

Added approved stamp design with stars around circle edge and diagonal stripes for DONE status.

Fixed Shape modifier chain - `.fill()` must come before `.frame()` when using custom Shape.

Added `onEssayTap` closure to MonthCardView for navigating to essay detail when tapping back of written cards.

---

## Memory Flush - April 21, 2026 06:49 AM

### DailyWrite Keyword Stability Fix

**Problem:** Keywords were changing position every time the app launched because `String.hashValue` is not stable across app launches.

**Solution:** Replaced unstable hash with stable date-based calculation:
```swift
// Old (unstable):
let poolIndex = abs(dateString.hashValue) % 100

// New (stable):
let components = calendar.dateComponents([.year, .month, .day], from: date)
let poolIndex = ((components.year! * 10000) + (components.month! * 100) + components.day!) % 100
```

**Files Modified:** DailyPromptView.swift - `generateMonthCards()` function

### DailyWrite Card Back Design

User preference confirmed: Back of cards should have **cream background** with **colored text** matching the front card color (not cream text on colored background).

This creates a nice visual effect where the card's state color (Indigo/Moss/Ruby/Burnt) carries through to the back via the text color.

---

## DailyWrite Debugging Session - Card Color Not Updating (09:49)

**Problem:** After writing an essay, the card color doesn't change to Moss Shadow (green/brown "done" state). It stays blue (Indigo Rain) or shows orange (Burnt Nectar).

**Investigation:**
1. Checked `cardBackground` computed property - order was wrong: `isFuture` was checked before `isWritten`
2. Fixed priority: `isWritten` now checked first, then `isFuture`, then `!isToday`, then default

**Root Cause Found:**
The `isWritten` calculation uses `essays[card.keyword]` dictionary lookup. Debug logs showed:
- 27 essays loaded from Firestore
- Multiple essays for same keyword (e.g., 4 essays for "지혜")
- Dictionary only keeps last essay per keyword
- Some essays have `deletedAt` set (soft deleted)
- If the last essay for a keyword is deleted, `isWritten` returns false even if non-deleted essays exist

**Fix Applied:**
Modified `loadEssaysForMonth()` to prefer non-deleted essays when building dictionary:
```swift
// Only add if no existing essay OR existing is deleted and this one isn't
if existing == nil || (existing?.deletedAt != nil && essay.deletedAt == nil) {
    essayDict[keyword] = essay
}
```

**Additional Issues Found:**
1. Delete functionality not working from essay detail view
2. User reported: "Already has essay" blocking access to writing even after deletion

**Status:** Ongoing debugging. User needs to test if card color updates after refresh.

---

## DailyWrite Delete Function Investigation (10:06)

**Problem:** Delete button in essay detail view (KeywordArchiveView.swift) not working. User taps "..." menu → Delete → nothing happens.

**Console Output:**
```
Called -[UIContextMenuInteraction updateVisibleMenuWithBlock:] while no context menu is visible. This won't do anything.
```

**Analysis:**
- The UI warning suggests the context menu (the "..." button menu) isn't showing properly
- The delete function `softDeleteEssay()` exists and looks correct
- The alert shows with "Move to Recently Deleted?" message
- But the actual delete doesn't seem to execute

**Next Steps:**
1. Verify the Menu button is working (the "..." ellipsis in top right)
2. Check if the alert confirmation is being tapped
3. Verify `deleteEssay()` async function is being called
4. Check Firebase console for actual delete operations

**Files Involved:**
- KeywordArchiveView.swift (essay detail view with delete button)
- FirebaseService.swift (softDeleteEssay function)
- DailyPromptView.swift (card color logic, dictionary building)

---


---

## DailyWrite Tab Bar Redesign & StreaksView (10:30-10:53)

**New 5-Tab Layout:**
1. **Home** - Card view (DailyPromptView with monthly flip cards)
2. **Streaks** - Monthly dots calendar showing writing activity
3. **+** (Center) - Prominent circular button for writing today's essay
4. **Feed** - Existing feed view
5. **Profile** - Existing profile view

**Custom Tab Bar Implementation:**
- Replaced standard TabView with ZStack overlay containing custom tab bar
- Center + button elevated with shadow (56pt circle, Indigo Rain background)
- Tab bar background: Cream (#F5F0E8) with subtle top border
- Icons: house, flame.fill, plus, newspaper, person

**StreaksView Features:**
- Streak badge with flame icon (Burnt Nectar color)
- Monthly dots calendar (30-day grid with rounded rectangles)
- Stats: Words Written, Published count
- Color scheme matching card states:
  - Moss Shadow (#4A5A30) - Written days
  - Indigo Rain (#0D244D) - Today (if not written)
  - Ruby Leaf (#852E47) - Missed days
  - Burnt Nectar (#C2441C) - Future days
  - Yellow ring - Around today if not yet written

**Files Modified:**
- ContentView.swift - Complete MainTabView rewrite with custom tab bar
- Added StreaksView, MonthlyStreakView, StreakDayCell
- Added WritingSheetView for center + button

**Technical Notes:**
- Center button opens WritingSheet with today's keyword pre-loaded
- Uses NotificationCenter to trigger reload when essay deleted
- StreaksView uses original MonthlyStreakView code (rounded rectangles, not dots)
- Color priority matches card design: written = green, today empty = navy, missed = ruby, future = burnt

---

## DailyWrite Day of Week Display (10:54)

Added day of week abbreviation (MON, TUE, WED...) next to the big date number on cards.

**Implementation:**
- Added `dayOfWeekString(from:)` helper in MonthCardView
- Uses DateFormatter with "EEE" format for short weekday
- Placed next to day number in card header
- Uppercase for consistency with coffee label aesthetic

**Files Modified:**
- DailyPromptView.swift - Added dayOfWeekString function inside MonthCardView

---
# Memory - April 21, 2026

## DailyWrite App Development

### Card-Based Flip Design Implementation
Working on a major redesign of the DailyPromptView (Write tab) to use card-based flip interactions.

**Design Evolution:**
- Started with 3-column grid, pivoted to horizontal carousel, then settled on vertical scroll with full-width cards
- Final design: Vintage ticket-style cards with flip animation
- Color palette changed from earth tones to vibrant tricolor: Pink (#C87E96) for missed, Teal (#C9414E) for done, Chartreuse (#D6D385) for empty

**Current Status:**
- Created FlipCardView.swift with 3D rotation animation
- Implemented ticket shape with perforated edges
- Front shows date + status (TODAY/MISSED/DONE stamp)
- Back shows keyword in coffee label style with word count, likes, comments stats
- Vertical scroll through 31 days (past 30 + today)

**Files Modified:**
- DailyPromptView.swift - Card grid layout
- FlipCardView.swift - Ticket-style flip card component
- KeywordsService.swift - Added getKeywordForDate() helper

**Next Steps:**
- Test build and fix any remaining compile errors
- Move streak/dot calendar to ProfileView
- Polish animations and interactions


---

## Later Session - Monthly Card Deck with Coffee Label Aesthetic

**Palette Update:**
User provided a new sophisticated color palette from Jenny Henderson Studio:
- Navy (#374375) - Primary dark
- Cloud (#FFFCF5) - Off-white/cream
- Periwinkle (#BABDE2) - Soft blue-lavender  
- Maroon (#895159) - Deep reddish-brown
- Peach (#DFAAA1) - Soft pink-beige

**Design Pivot:**
Pivoted to coffee label aesthetic inspired by coffee packaging - clean, elegant typography with:
- Thin horizontal dividers (0.5pt lines)
- Grid layout for details (MONTH / STATUS / WRITING)
- Large day numbers with small uppercase date labels
- Circular status stamps (checkmark, TODAY, LOCKED)
- Sharp 4pt corners (more refined than rounded cards)
- Subtle shadows for depth

**Architecture Changes:**
- Complete rewrite of DailyPromptView.swift
- Monthly deck with horizontal TabView for month swiping
- Vertical ScrollView for days 1-31 within each month
- Future days locked (not flippable, keyword secret)
- Auto-scroll to today's card on load
- Back card shows keyword in quotes with write button or stats

**Color Mapping Updated:**
- Navy (#374375) - Done/Written cards + accents
- Maroon (#895159) - Missed/Past empty cards
- Cloud (#FFFCF5) - Empty/Future cards + background + back side

**Fixes Applied:**
- Fixed duplicate Color.init(hex:) extension in FlipCardView.swift
- Added getKeywordForDate(date:language:) to KeywordsService.swift
- Updated DailyPromptView to use KeywordsService instead of KeywordGenerator for per-date keywords

**Time:** 04:44 KST (April 21)

## DailyWrite Color Palette Update (05:32)

Updated card color scheme:
- **Burnt Nectar** (#C2441C) - Future cards
- **Moss Shadow** (#AA542B) - Done/completed cards
- **Ruby Leaf** (#852E47) - Missed days
- **Cream** (#F5F0E8) - Today's card (empty/active state)

Added approved stamp design with stars around circle edge and diagonal stripes for DONE status.

Fixed Shape modifier chain - `.fill()` must come before `.frame()` when using custom Shape.

Added `onEssayTap` closure to MonthCardView for navigating to essay detail when tapping back of written cards.

---

## Memory Flush - April 21, 2026 06:49 AM

### DailyWrite Keyword Stability Fix

**Problem:** Keywords were changing position every time the app launched because `String.hashValue` is not stable across app launches.

**Solution:** Replaced unstable hash with stable date-based calculation:
```swift
// Old (unstable):
let poolIndex = abs(dateString.hashValue) % 100

// New (stable):
let components = calendar.dateComponents([.year, .month, .day], from: date)
let poolIndex = ((components.year! * 10000) + (components.month! * 100) + components.day!) % 100
```

**Files Modified:** DailyPromptView.swift - `generateMonthCards()` function

### DailyWrite Card Back Design

User preference confirmed: Back of cards should have **cream background** with **colored text** matching the front card color (not cream text on colored background).

This creates a nice visual effect where the card's state color (Indigo/Moss/Ruby/Burnt) carries through to the back via the text color.

---

## DailyWrite Debugging Session - Card Color Not Updating (09:49)

**Problem:** After writing an essay, the card color doesn't change to Moss Shadow (green/brown "done" state). It stays blue (Indigo Rain) or shows orange (Burnt Nectar).

**Investigation:**
1. Checked `cardBackground` computed property - order was wrong: `isFuture` was checked before `isWritten`
2. Fixed priority: `isWritten` now checked first, then `isFuture`, then `!isToday`, then default

**Root Cause Found:**
The `isWritten` calculation uses `essays[card.keyword]` dictionary lookup. Debug logs showed:
- 27 essays loaded from Firestore
- Multiple essays for same keyword (e.g., 4 essays for "지혜")
- Dictionary only keeps last essay per keyword
- Some essays have `deletedAt` set (soft deleted)
- If the last essay for a keyword is deleted, `isWritten` returns false even if non-deleted essays exist

**Fix Applied:**
Modified `loadEssaysForMonth()` to prefer non-deleted essays when building dictionary:
```swift
// Only add if no existing essay OR existing is deleted and this one isn't
if existing == nil || (existing?.deletedAt != nil && essay.deletedAt == nil) {
    essayDict[keyword] = essay
}
```

**Additional Issues Found:**
1. Delete functionality not working from essay detail view
2. User reported: "Already has essay" blocking access to writing even after deletion

**Status:** Ongoing debugging. User needs to test if card color updates after refresh.

---

## DailyWrite Delete Function Investigation (10:06)

**Problem:** Delete button in essay detail view (KeywordArchiveView.swift) not working. User taps "..." menu → Delete → nothing happens.

**Console Output:**
```
Called -[UIContextMenuInteraction updateVisibleMenuWithBlock:] while no context menu is visible. This won't do anything.
```

**Analysis:**
- The UI warning suggests the context menu (the "..." button menu) isn't showing properly
- The delete function `softDeleteEssay()` exists and looks correct
- The alert shows with "Move to Recently Deleted?" message
- But the actual delete doesn't seem to execute

**Next Steps:**
1. Verify the Menu button is working (the "..." ellipsis in top right)
2. Check if the alert confirmation is being tapped
3. Verify `deleteEssay()` async function is being called
4. Check Firebase console for actual delete operations

**Files Involved:**
- KeywordArchiveView.swift (essay detail view with delete button)
- FirebaseService.swift (softDeleteEssay function)
- DailyPromptView.swift (card color logic, dictionary building)

---

---

## DailyWrite Tab Bar Redesign & StreaksView (10:30-10:53)

**New 5-Tab Layout:**
1. **Home** - Card view (DailyPromptView with monthly flip cards)
2. **Streaks** - Monthly dots calendar showing writing activity
3. **+** (Center) - Prominent circular button for writing today's essay
4. **Feed** - Existing feed view
5. **Profile** - Existing profile view

**Custom Tab Bar Implementation:**
- Replaced standard TabView with ZStack overlay containing custom tab bar
- Center + button elevated with shadow (56pt circle, Indigo Rain background)
- Tab bar background: Cream (#F5F0E8) with subtle top border
- Icons: house, flame.fill, plus, newspaper, person

**StreaksView Features:**
- Streak badge with flame icon (Burnt Nectar color)
- Monthly dots calendar (30-day grid with rounded rectangles)
- Stats: Words Written, Published count
- Color scheme matching card states:
  - Moss Shadow (#4A5A30) - Written days
  - Indigo Rain (#0D244D) - Today (if not written)
  - Ruby Leaf (#852E47) - Missed days
  - Burnt Nectar (#C2441C) - Future days
  - Yellow ring - Around today if not yet written

**Files Modified:**
- ContentView.swift - Complete MainTabView rewrite with custom tab bar
- Added StreaksView, MonthlyStreakView, StreakDayCell
- Added WritingSheetView for center + button

**Technical Notes:**
- Center button opens WritingSheet with today's keyword pre-loaded
- Uses NotificationCenter to trigger reload when essay deleted
- StreaksView uses original MonthlyStreakView code (rounded rectangles, not dots)
- Color priority matches card design: written = green, today empty = navy, missed = ruby, future = burnt

---

## DailyWrite Day of Week Display (10:54)

Added day of week abbreviation (MON, TUE, WED...) next to the big date number on cards.

**Implementation:**
- Added `dayOfWeekString(from:)` helper in MonthCardView
- Uses DateFormatter with "EEE" format for short weekday
- Placed next to day number in card header
- Uppercase for consistency with coffee label aesthetic

**Files Modified:**
- DailyPromptView.swift - Added dayOfWeekString function inside MonthCardView

---

## DailyWrite Tab Bar & Theme Cleanup (11:15-11:25)

### Tab Bar Layout Fix
**Problem:** Content at bottom of screen was being cut off awkwardly by custom tab bar.

**Solution:**
- Removed floating effect from center + button (now inline with other tabs)
- Tab bar extends fully to bottom edge with `ignoresSafeArea(edges: .bottom)`
- Added 100pt bottom padding to content views so they don't get hidden
- Tab bar background: Cream (#F5F0E8) with subtle top border line

**Files Modified:**
- ContentView.swift - Updated customTabBar to remove floating, extend to bottom
- DailyPromptView.swift - Added `.padding(.bottom, 100)` to monthlyCardDeckView
- FeedView.swift - Added bottom padding
- ProfileView.swift - Added bottom padding

### Theme Simplification
**Changes:**
- Fixed accent color to **Indigo Rain** (#0D244D) - no more switching
- Removed light/dark theme toggle - app now always uses light theme
- Deleted `ThemePickerView.swift` entirely
- Removed `AccentColorPickerView` from `SettingsView.swift`
- Simplified `ThemeManager.swift` to only support light theme + Indigo Rain accent
- Removed theme picker references from Xcode project file

**Files Modified:**
- ThemeManager.swift - Simplified to fixed light theme + Indigo Rain accent
- DailyWriteApp.swift - Fixed `.tint()` and `.accentColor()` to Indigo Rain
- ContentView.swift - Removed `preferredColorScheme` binding
- SettingsView.swift - Removed theme and accent color pickers from UI
- Deleted ThemePickerView.swift

**Result:** Cleaner, simpler app with consistent coffee label aesthetic throughout.

---
# Memory - April 21, 2026

## DailyWrite App Development

### Card-Based Flip Design Implementation
Working on a major redesign of the DailyPromptView (Write tab) to use card-based flip interactions.

**Design Evolution:**
- Started with 3-column grid, pivoted to horizontal carousel, then settled on vertical scroll with full-width cards
- Final design: Vintage ticket-style cards with flip animation
- Color palette changed from earth tones to vibrant tricolor: Pink (#C87E96) for missed, Teal (#C9414E) for done, Chartreuse (#D6D385) for empty

**Current Status:**
- Created FlipCardView.swift with 3D rotation animation
- Implemented ticket shape with perforated edges
- Front shows date + status (TODAY/MISSED/DONE stamp)
- Back shows keyword in coffee label style with word count, likes, comments stats
- Vertical scroll through 31 days (past 30 + today)

**Files Modified:**
- DailyPromptView.swift - Card grid layout
- FlipCardView.swift - Ticket-style flip card component
- KeywordsService.swift - Added getKeywordForDate() helper

**Next Steps:**
- Test build and fix any remaining compile errors
- Move streak/dot calendar to ProfileView
- Polish animations and interactions


---

## Later Session - Monthly Card Deck with Coffee Label Aesthetic

**Palette Update:**
User provided a new sophisticated color palette from Jenny Henderson Studio:
- Navy (#374375) - Primary dark
- Cloud (#FFFCF5) - Off-white/cream
- Periwinkle (#BABDE2) - Soft blue-lavender  
- Maroon (#895159) - Deep reddish-brown
- Peach (#DFAAA1) - Soft pink-beige

**Design Pivot:**
Pivoted to coffee label aesthetic inspired by coffee packaging - clean, elegant typography with:
- Thin horizontal dividers (0.5pt lines)
- Grid layout for details (MONTH / STATUS / WRITING)
- Large day numbers with small uppercase date labels
- Circular status stamps (checkmark, TODAY, LOCKED)
- Sharp 4pt corners (more refined than rounded cards)
- Subtle shadows for depth

**Architecture Changes:**
- Complete rewrite of DailyPromptView.swift
- Monthly deck with horizontal TabView for month swiping
- Vertical ScrollView for days 1-31 within each month
- Future days locked (not flippable, keyword secret)
- Auto-scroll to today's card on load
- Back card shows keyword in quotes with write button or stats

**Color Mapping Updated:**
- Navy (#374375) - Done/Written cards + accents
- Maroon (#895159) - Missed/Past empty cards
- Cloud (#FFFCF5) - Empty/Future cards + background + back side

**Fixes Applied:**
- Fixed duplicate Color.init(hex:) extension in FlipCardView.swift
- Added getKeywordForDate(date:language:) to KeywordsService.swift
- Updated DailyPromptView to use KeywordsService instead of KeywordGenerator for per-date keywords

**Time:** 04:44 KST (April 21)

## DailyWrite Color Palette Update (05:32)

Updated card color scheme:
- **Burnt Nectar** (#C2441C) - Future cards
- **Moss Shadow** (#AA542B) - Done/completed cards
- **Ruby Leaf** (#852E47) - Missed days
- **Cream** (#F5F0E8) - Today's card (empty/active state)

Added approved stamp design with stars around circle edge and diagonal stripes for DONE status.

Fixed Shape modifier chain - `.fill()` must come before `.frame()` when using custom Shape.

Added `onEssayTap` closure to MonthCardView for navigating to essay detail when tapping back of written cards.

---

## Memory Flush - April 21, 2026 06:49 AM

### DailyWrite Keyword Stability Fix

**Problem:** Keywords were changing position every time the app launched because `String.hashValue` is not stable across app launches.

**Solution:** Replaced unstable hash with stable date-based calculation:
```swift
// Old (unstable):
let poolIndex = abs(dateString.hashValue) % 100

// New (stable):
let components = calendar.dateComponents([.year, .month, .day], from: date)
let poolIndex = ((components.year! * 10000) + (components.month! * 100) + components.day!) % 100
```

**Files Modified:** DailyPromptView.swift - `generateMonthCards()` function

### DailyWrite Card Back Design

User preference confirmed: Back of cards should have **cream background** with **colored text** matching the front card color (not cream text on colored background).

This creates a nice visual effect where the card's state color (Indigo/Moss/Ruby/Burnt) carries through to the back via the text color.

---

## DailyWrite Debugging Session - Card Color Not Updating (09:49)

**Problem:** After writing an essay, the card color doesn't change to Moss Shadow (green/brown "done" state). It stays blue (Indigo Rain) or shows orange (Burnt Nectar).

**Investigation:**
1. Checked `cardBackground` computed property - order was wrong: `isFuture` was checked before `isWritten`
2. Fixed priority: `isWritten` now checked first, then `isFuture`, then `!isToday`, then default

**Root Cause Found:**
The `isWritten` calculation uses `essays[card.keyword]` dictionary lookup. Debug logs showed:
- 27 essays loaded from Firestore
- Multiple essays for same keyword (e.g., 4 essays for "지혜")
- Dictionary only keeps last essay per keyword
- Some essays have `deletedAt` set (soft deleted)
- If the last essay for a keyword is deleted, `isWritten` returns false even if non-deleted essays exist

**Fix Applied:**
Modified `loadEssaysForMonth()` to prefer non-deleted essays when building dictionary:
```swift
// Only add if no existing essay OR existing is deleted and this one isn't
if existing == nil || (existing?.deletedAt != nil && essay.deletedAt == nil) {
    essayDict[keyword] = essay
}
```

**Additional Issues Found:**
1. Delete functionality not working from essay detail view
2. User reported: "Already has essay" blocking access to writing even after deletion

**Status:** Ongoing debugging. User needs to test if card color updates after refresh.

---

## DailyWrite Delete Function Investigation (10:06)

**Problem:** Delete button in essay detail view (KeywordArchiveView.swift) not working. User taps "..." menu → Delete → nothing happens.

**Console Output:**
```
Called -[UIContextMenuInteraction updateVisibleMenuWithBlock:] while no context menu is visible. This won't do anything.
```

**Analysis:**
- The UI warning suggests the context menu (the "..." button menu) isn't showing properly
- The delete function `softDeleteEssay()` exists and looks correct
- The alert shows with "Move to Recently Deleted?" message
- But the actual delete doesn't seem to execute

**Next Steps:**
1. Verify the Menu button is working (the "..." ellipsis in top right)
2. Check if the alert confirmation is being tapped
3. Verify `deleteEssay()` async function is being called
4. Check Firebase console for actual delete operations

**Files Involved:**
- KeywordArchiveView.swift (essay detail view with delete button)
- FirebaseService.swift (softDeleteEssay function)
- DailyPromptView.swift (card color logic, dictionary building)

---


---

## DailyWrite Tab Bar Redesign & StreaksView (10:30-10:53)

**New 5-Tab Layout:**
1. **Home** - Card view (DailyPromptView with monthly flip cards)
2. **Streaks** - Monthly dots calendar showing writing activity
3. **+** (Center) - Prominent circular button for writing today's essay
4. **Feed** - Existing feed view
5. **Profile** - Existing profile view

**Custom Tab Bar Implementation:**
- Replaced standard TabView with ZStack overlay containing custom tab bar
- Center + button elevated with shadow (56pt circle, Indigo Rain background)
- Tab bar background: Cream (#F5F0E8) with subtle top border
- Icons: house, flame.fill, plus, newspaper, person

**StreaksView Features:**
- Streak badge with flame icon (Burnt Nectar color)
- Monthly dots calendar (30-day grid with rounded rectangles)
- Stats: Words Written, Published count
- Color scheme matching card states:
  - Moss Shadow (#4A5A30) - Written days
  - Indigo Rain (#0D244D) - Today (if not written)
  - Ruby Leaf (#852E47) - Missed days
  - Burnt Nectar (#C2441C) - Future days
  - Yellow ring - Around today if not yet written

**Files Modified:**
- ContentView.swift - Complete MainTabView rewrite with custom tab bar
- Added StreaksView, MonthlyStreakView, StreakDayCell
- Added WritingSheetView for center + button

**Technical Notes:**
- Center button opens WritingSheet with today's keyword pre-loaded
- Uses NotificationCenter to trigger reload when essay deleted
- StreaksView uses original MonthlyStreakView code (rounded rectangles, not dots)
- Color priority matches card design: written = green, today empty = navy, missed = ruby, future = burnt

---

## DailyWrite Day of Week Display (10:54)

Added day of week abbreviation (MON, TUE, WED...) next to the big date number on cards.

**Implementation:**
- Added `dayOfWeekString(from:)` helper in MonthCardView
- Uses DateFormatter with "EEE" format for short weekday
- Placed next to day number in card header
- Uppercase for consistency with coffee label aesthetic

**Files Modified:**
- DailyPromptView.swift - Added dayOfWeekString function inside MonthCardView

---
# Memory - April 21, 2026

## DailyWrite App Development

### Card-Based Flip Design Implementation
Working on a major redesign of the DailyPromptView (Write tab) to use card-based flip interactions.

**Design Evolution:**
- Started with 3-column grid, pivoted to horizontal carousel, then settled on vertical scroll with full-width cards
- Final design: Vintage ticket-style cards with flip animation
- Color palette changed from earth tones to vibrant tricolor: Pink (#C87E96) for missed, Teal (#C9414E) for done, Chartreuse (#D6D385) for empty

**Current Status:**
- Created FlipCardView.swift with 3D rotation animation
- Implemented ticket shape with perforated edges
- Front shows date + status (TODAY/MISSED/DONE stamp)
- Back shows keyword in coffee label style with word count, likes, comments stats
- Vertical scroll through 31 days (past 30 + today)

**Files Modified:**
- DailyPromptView.swift - Card grid layout
- FlipCardView.swift - Ticket-style flip card component
- KeywordsService.swift - Added getKeywordForDate() helper

**Next Steps:**
- Test build and fix any remaining compile errors
- Move streak/dot calendar to ProfileView
- Polish animations and interactions


---

## Later Session - Monthly Card Deck with Coffee Label Aesthetic

**Palette Update:**
User provided a new sophisticated color palette from Jenny Henderson Studio:
- Navy (#374375) - Primary dark
- Cloud (#FFFCF5) - Off-white/cream
- Periwinkle (#BABDE2) - Soft blue-lavender  
- Maroon (#895159) - Deep reddish-brown
- Peach (#DFAAA1) - Soft pink-beige

**Design Pivot:**
Pivoted to coffee label aesthetic inspired by coffee packaging - clean, elegant typography with:
- Thin horizontal dividers (0.5pt lines)
- Grid layout for details (MONTH / STATUS / WRITING)
- Large day numbers with small uppercase date labels
- Circular status stamps (checkmark, TODAY, LOCKED)
- Sharp 4pt corners (more refined than rounded cards)
- Subtle shadows for depth

**Architecture Changes:**
- Complete rewrite of DailyPromptView.swift
- Monthly deck with horizontal TabView for month swiping
- Vertical ScrollView for days 1-31 within each month
- Future days locked (not flippable, keyword secret)
- Auto-scroll to today's card on load
- Back card shows keyword in quotes with write button or stats

**Color Mapping Updated:**
- Navy (#374375) - Done/Written cards + accents
- Maroon (#895159) - Missed/Past empty cards
- Cloud (#FFFCF5) - Empty/Future cards + background + back side

**Fixes Applied:**
- Fixed duplicate Color.init(hex:) extension in FlipCardView.swift
- Added getKeywordForDate(date:language:) to KeywordsService.swift
- Updated DailyPromptView to use KeywordsService instead of KeywordGenerator for per-date keywords

**Time:** 04:44 KST (April 21)

## DailyWrite Color Palette Update (05:32)

Updated card color scheme:
- **Burnt Nectar** (#C2441C) - Future cards
- **Moss Shadow** (#AA542B) - Done/completed cards
- **Ruby Leaf** (#852E47) - Missed days
- **Cream** (#F5F0E8) - Today's card (empty/active state)

Added approved stamp design with stars around circle edge and diagonal stripes for DONE status.

Fixed Shape modifier chain - `.fill()` must come before `.frame()` when using custom Shape.

Added `onEssayTap` closure to MonthCardView for navigating to essay detail when tapping back of written cards.

---

## Memory Flush - April 21, 2026 06:49 AM

### DailyWrite Keyword Stability Fix

**Problem:** Keywords were changing position every time the app launched because `String.hashValue` is not stable across app launches.

**Solution:** Replaced unstable hash with stable date-based calculation:
```swift
// Old (unstable):
let poolIndex = abs(dateString.hashValue) % 100

// New (stable):
let components = calendar.dateComponents([.year, .month, .day], from: date)
let poolIndex = ((components.year! * 10000) + (components.month! * 100) + components.day!) % 100
```

**Files Modified:** DailyPromptView.swift - `generateMonthCards()` function

### DailyWrite Card Back Design

User preference confirmed: Back of cards should have **cream background** with **colored text** matching the front card color (not cream text on colored background).

This creates a nice visual effect where the card's state color (Indigo/Moss/Ruby/Burnt) carries through to the back via the text color.

---

## DailyWrite Debugging Session - Card Color Not Updating (09:49)

**Problem:** After writing an essay, the card color doesn't change to Moss Shadow (green/brown "done" state). It stays blue (Indigo Rain) or shows orange (Burnt Nectar).

**Investigation:**
1. Checked `cardBackground` computed property - order was wrong: `isFuture` was checked before `isWritten`
2. Fixed priority: `isWritten` now checked first, then `isFuture`, then `!isToday`, then default

**Root Cause Found:**
The `isWritten` calculation uses `essays[card.keyword]` dictionary lookup. Debug logs showed:
- 27 essays loaded from Firestore
- Multiple essays for same keyword (e.g., 4 essays for "지혜")
- Dictionary only keeps last essay per keyword
- Some essays have `deletedAt` set (soft deleted)
- If the last essay for a keyword is deleted, `isWritten` returns false even if non-deleted essays exist

**Fix Applied:**
Modified `loadEssaysForMonth()` to prefer non-deleted essays when building dictionary:
```swift
// Only add if no existing essay OR existing is deleted and this one isn't
if existing == nil || (existing?.deletedAt != nil && essay.deletedAt == nil) {
    essayDict[keyword] = essay
}
```

**Additional Issues Found:**
1. Delete functionality not working from essay detail view
2. User reported: "Already has essay" blocking access to writing even after deletion

**Status:** Ongoing debugging. User needs to test if card color updates after refresh.

---

## DailyWrite Delete Function Investigation (10:06)

**Problem:** Delete button in essay detail view (KeywordArchiveView.swift) not working. User taps "..." menu → Delete → nothing happens.

**Console Output:**
```
Called -[UIContextMenuInteraction updateVisibleMenuWithBlock:] while no context menu is visible. This won't do anything.
```

**Analysis:**
- The UI warning suggests the context menu (the "..." button menu) isn't showing properly
- The delete function `softDeleteEssay()` exists and looks correct
- The alert shows with "Move to Recently Deleted?" message
- But the actual delete doesn't seem to execute

**Next Steps:**
1. Verify the Menu button is working (the "..." ellipsis in top right)
2. Check if the alert confirmation is being tapped
3. Verify `deleteEssay()` async function is being called
4. Check Firebase console for actual delete operations

**Files Involved:**
- KeywordArchiveView.swift (essay detail view with delete button)
- FirebaseService.swift (softDeleteEssay function)
- DailyPromptView.swift (card color logic, dictionary building)

---


---

## DailyWrite Tab Bar Redesign & StreaksView (10:30-10:53)

**New 5-Tab Layout:**
1. **Home** - Card view (DailyPromptView with monthly flip cards)
2. **Streaks** - Monthly dots calendar showing writing activity
3. **+** (Center) - Prominent circular button for writing today's essay
4. **Feed** - Existing feed view
5. **Profile** - Existing profile view

**Custom Tab Bar Implementation:**
- Replaced standard TabView with ZStack overlay containing custom tab bar
- Center + button elevated with shadow (56pt circle, Indigo Rain background)
- Tab bar background: Cream (#F5F0E8) with subtle top border
- Icons: house, flame.fill, plus, newspaper, person

**StreaksView Features:**
- Streak badge with flame icon (Burnt Nectar color)
- Monthly dots calendar (30-day grid with rounded rectangles)
- Stats: Words Written, Published count
- Color scheme matching card states:
  - Moss Shadow (#4A5A30) - Written days
  - Indigo Rain (#0D244D) - Today (if not written)
  - Ruby Leaf (#852E47) - Missed days
  - Burnt Nectar (#C2441C) - Future days
  - Yellow ring - Around today if not yet written

**Files Modified:**
- ContentView.swift - Complete MainTabView rewrite with custom tab bar
- Added StreaksView, MonthlyStreakView, StreakDayCell
- Added WritingSheetView for center + button

**Technical Notes:**
- Center button opens WritingSheet with today's keyword pre-loaded
- Uses NotificationCenter to trigger reload when essay deleted
- StreaksView uses original MonthlyStreakView code (rounded rectangles, not dots)
- Color priority matches card design: written = green, today empty = navy, missed = ruby, future = burnt

---

## DailyWrite Day of Week Display (10:54)

Added day of week abbreviation (MON, TUE, WED...) next to the big date number on cards.

**Implementation:**
- Added `dayOfWeekString(from:)` helper in MonthCardView
- Uses DateFormatter with "EEE" format for short weekday
- Placed next to day number in card header
- Uppercase for consistency with coffee label aesthetic

**Files Modified:**
- DailyPromptView.swift - Added dayOfWeekString function inside MonthCardView

---

## DailyWrite Tab Bar & Theme Cleanup (11:15-11:25)

### Tab Bar Layout Fix
**Problem:** Content at bottom of screen was being cut off awkwardly by custom tab bar.

**Solution:**
- Removed floating effect from center + button (now inline with other tabs)
- Tab bar extends fully to bottom edge with `ignoresSafeArea(edges: .bottom)`
- Added 100pt bottom padding to content views so they don't get hidden
- Tab bar background: Cream (#F5F0E8) with subtle top border line

**Files Modified:**
- ContentView.swift - Updated customTabBar to remove floating, extend to bottom
- DailyPromptView.swift - Added `.padding(.bottom, 100)` to monthlyCardDeckView
- FeedView.swift - Added bottom padding
- ProfileView.swift - Added bottom padding

### Theme Simplification
**Changes:**
- Fixed accent color to **Indigo Rain** (#0D244D) - no more switching
- Removed light/dark theme toggle - app now always uses light theme
- Deleted `ThemePickerView.swift` entirely
- Removed `AccentColorPickerView` from `SettingsView.swift`
- Simplified `ThemeManager.swift` to only support light theme + Indigo Rain accent
- Removed theme picker references from Xcode project file

**Files Modified:**
- ThemeManager.swift - Simplified to fixed light theme + Indigo Rain accent
- DailyWriteApp.swift - Fixed `.tint()` and `.accentColor()` to Indigo Rain
- ContentView.swift - Removed `preferredColorScheme` binding
- SettingsView.swift - Removed theme and accent color pickers from UI
- Deleted ThemePickerView.swift

**Result:** Cleaner, simpler app with consistent coffee label aesthetic throughout.

---

## DailyWrite ProfileView Redesign (11:35-11:42)

### ProfileView Redesign
Redesigned ProfileView with coffee label aesthetic matching the rest of the app.

**Changes:**
- **Background:** Beige/cream (#F5F0E8) full-screen background
- **Menu buttons:** White cards with subtle shadow, Indigo Rain icons/text, 12pt rounded corners
- **Removed sections:** "내 글 전체보기" (Your Essays list) and "소개" (bio) - cleaner layout
- **Weekly statistics:** 7-day bar chart with Moss Shadow (#4A5A30) fill for active days, light Indigo background for empty days
- **Show all button:** Added "Show all" button in weekly stats header that navigates to WritingAnalyticsView
- **Header colors:** Updated ProfileHeader and ProfileStatView to use Indigo Rain (#0D244D) for all text and accents

**Files Modified:**
- ProfileView.swift - Complete redesign with beige background, white menu cards, weekly stats
- ProfileHeader - Changed `isFollowing` from `let` to `@Binding` to properly reflect follow state changes
- EditProfileView - Removed bio (소개) section from edit form

**Bug Fixes:**
- Fixed ProfileView.swift line 42 missing closing brace causing `monthHeader` scope error
- Fixed ProfileView.swift line 665 optional binding error with `calendar.date(byAdding:)` - changed `if let` to `guard let`
- Fixed ProfileView.swift line 198 - `essay.createdAt` is not optional, removed `guard let`

---

## DailyWrite KeywordArchiveView Redesign (11:40-11:42)

### Writing Archive Calendar Redesign
Completely redesigned the 글 보관함 (Writing Archive) with a clean calendar view inspired by event planning apps.

**Design Features:**
- **Cream background** (#F5F0E8) matching app aesthetic
- **Month header** with circular navigation buttons (chevrons in white circles)
- **Calendar card** - White rounded card with shadow containing:
  - Weekday headers (Su Mo Tu We Th Fr Sa)
  - 7-column grid with day numbers
  - **Status dots** below each day:
    - 🟢 Moss green - Written
    - 🔵 Indigo - Today (empty)
    - 🔴 Ruby - Missed
    - 🟠 Burnt - Future
- **Selected day card** - White card showing:
  - Date with day of week (e.g., "Apr 21, Monday")
  - Keyword for that day
  - Status badge (Written/Today/Missed/Upcoming)
  - Essay list or empty state with "Write now" button
- **Essay cards** - Clean list items with emoji, title, word count, likes

**Files Created:**
- EssayDetailView.swift - New view for displaying individual essays with header, content, stats, edit/delete options

**Files Modified:**
- KeywordArchiveView.swift - Complete rewrite with new calendar design
- DailyWrite.xcodeproj/project.pbxproj - Added EssayDetailView.swift to project

---


---

## DailyWrite EssayDetailView & Comments Redesign (12:00-12:09)

### EssayDetailView Redesign
Redesigned the essay detail view with cleaner aesthetic matching the app design.

**Changes:**
- **Page background:** Beige/cream (#F5F0E8) for the entire view
- **Header section:** Clean layout with keyword badge, title with indigo line separator (no box), author info
- **Content section:** Paper texture background (grid + speckles) with rounded corners, thin indigo border
- **Stats section:** White card with likes, comments count, word count, visibility icon

**Files Modified:**
- EssayDetailView.swift - Complete layout redesign with paper texture for content only

### Comments Inline Display
Changed from sheet-based comments to inline display in EssayDetailView.

**Changes:**
- Comments now show below the stats section without tapping
- Clean white card with "Comments" header and count
- Each comment shows author name, timestamp, and content
- Input field at bottom for adding new comments
- Removed CommentsView sheet - all inline now

**Files Modified:**
- EssayDetailView.swift - Added commentsSection, loadComments(), postComment()
- FeedView.swift - Added PaperTextureBackground struct (shared), updated CommentsView styling

### FeedView Simplification
Removed "Today" and "By Topic" tabs from FeedView.

**Changes:**
- Only 2 tabs now: "Following" and "Recent"
- Removed Today filter (case 1) and By Topic filter (case 3)
- Removed TopicSort enum, topicGroups state, selectedTopic state
- Removed getTopicGroups() and getEssaysForKeyword() functions
- Updated sort controls to show for filter 0 or 1
- Cleaned up swipe gesture handlers

**Files Modified:**
- FeedView.swift - Simplified to 2-tab layout

---

## DailyWrite Build Fixes (12:01-12:09)

### Duplicate Declarations Fixed
Fixed multiple compilation errors from code refactoring:

1. **EssayDetailView.swift line 19:** Duplicate `@State` declarations for `comments` and `newComment`
   - Removed duplicate declarations

2. **EssayDetailView.swift line 457:** Duplicate `PaperTextureBackground` struct
   - Removed from EssayDetailView.swift (kept in FeedView.swift as shared component)

3. **EssayDetailView.swift:** Removed unused `showingComments` @State variable
   - Comments now inline, no sheet needed

**Result:** Clean build with no redeclaration errors.

---

---

## DailyWrite Comments Threading Fix (12:20-12:27)

### Problem: 대댓글 (Nested Replies) Not Working
Comments were displaying but nested replies (대댓글) were not showing up.

**Root Cause:**
1. Optional String comparison issue: `parentCommentId` and `comment.id` are both optional, but direct comparison wasn't working
2. Reply posting wasn't implemented - the reply button just closed the field without posting

**Fixes Applied:**

1. **Fixed reply filtering** in commentsSection:
```swift
// Before (not working):
let replies = comments.filter { $0.parentCommentId == comment.id }

// After (working):
let replies = comments.filter { 
    if let replyParentId = $0.parentCommentId, 
       let commentId = comment.id {
        return replyParentId == commentId
    }
    return false
}
```

2. **Added onReply callback** to CommentRowView:
```swift
struct CommentRowView: View {
    let comment: Comment
    let replies: [Comment]
    let onReply: (String) -> Void  // Added callback
    // ...
}
```

3. **Implemented postReply function** in main view:
```swift
private func postReply(parentCommentId: String, content: String) {
    guard let essayId = essay.id, !content.isEmpty else { return }
    
    Task {
        do {
            try await FirebaseService.shared.addComment(
                essayId: essayId,
                content: content,
                parentCommentId: parentCommentId
            )
            await loadComments()  // Refresh to show new reply
        } catch {
            print("Error posting reply: \(error)")
        }
    }
}
```

4. **Reply button now posts** instead of just closing:
```swift
Button {
    onReply(replyText)  // Actually posts the reply
    showReplyField = false
    replyText = ""
} label: {
    Image(systemName: "arrow.up.circle.fill")
}
```

**Result:** Nested replies now display properly under their parent comments, and users can post new replies that persist to Firebase.

**Files Modified:**
- EssayDetailView.swift - Updated CommentRowView with onReply callback, added postReply function, fixed reply filtering

---
# Memory Log - 2026-04-21

## Session Summary

### FeedView Essay Card Redesign
- User shared reference image of clean paper card aesthetic (white paper with subtle shadow on beige background)
- EssayCard already updated with paper aesthetic matching the reference:
  - Paper color: #FDFBF7 (slightly brighter than page background #F5F0E8)
  - Subtle shadow on right/bottom (color: black opacity 0.08, radius: 12, offset: x:4 y:4)
  - Sharp corners (cornerRadius: 2) for clean paper look
  - Paper edge highlight (top/left) with white opacity 0.5
  - Indigo Rain (#0D244D) text throughout
  - Section dividers with subtle opacity

### EssayDetailView Improvements
- Fixed word count icon from non-existent `text.word.count` to `textformat` (SF Symbols)
- Stats section now shows consistent icons:
  - Heart for likes
  - Bubble for comments
  - Textformat for word count
  - Globe/Lock for visibility

### Font Preservation in EssayDetailView
- Added AttributedTextView (UIViewRepresentable wrapping UITextView) to properly display rich text formatting
- Maps AppFont rawValues to actual font file names (e.g., "KoPub Batang" → "KoPubBatang-Regular")
- Supports all AppFont cases with proper line spacing from essay settings
- Falls back to plain Text when no attributed content exists

### Current Status
- All major UI redesigns completed (DailyPromptView, FeedView, ProfileView, KeywordArchiveView, EssayDetailView)
- Comments threading (대댓글) implemented with proper parent-child relationships
- Paper texture with masking tape aesthetic applied to essay content
- Build ready for testing

---
**Last Updated:** 2026-04-21 12:42 PM (Asia/Seoul)


---

## DailyWrite FeedView Layout Fix (12:47)

### Problem: Essay Content Overflow
Essay content in EssayDetailView was spreading too wide beyond screen bounds.

### Solution Applied to AttributedTextView:
1. Added `widthTracksTextView = true` to text container
2. Set horizontal compression resistance to `.defaultLow`
3. Implemented `sizeThatFits` to calculate intrinsic size with width constraints (accounting for 56pt padding)

### Solution Applied to contentSection:
1. Added explicit `frame(maxWidth: .infinity)` to AttributedTextView and Text fallback
2. Added `alignment: .topLeading` to ZStack for proper anchoring

**Files Modified:** EssayDetailView.swift

---

## DailyWrite FeedView Build Fixes (12:47)

### Task Ambiguity Fix
Fixed "Use of 'Task' refers to instance method rather than generic struct 'Task' in module '_Concurrency'" errors:
- Changed all standalone `Task { }` to `_Concurrency.Task { }` to disambiguate
- `group.addTask` is part of TaskGroup API and doesn't need prefix

### Struct Scope Fix
Fixed "'private' can only be used in a non-local scope" errors:
- Helper functions (`navigateToEssay`, `loadEssays`, `enrichWithAuthors`, etc.) were inside the body due to missing closing braces
- Added proper closing braces after line 176 to close body and keep functions at struct level
- Removed orphaned Task block that was leftover from old EssayCard implementation

### Brace Balance
Verified all braces balanced (155 open, 155 close) - file now parses successfully.

**Files Modified:** FeedView.swift

---

**Last Updated:** 2026-04-21 12:57 PM (Asia/Seoul)
# Memory - April 21, 2026

## DailyWrite App Development

### Card-Based Flip Design Implementation
Working on a major redesign of the DailyPromptView (Write tab) to use card-based flip interactions.

**Design Evolution:**
- Started with 3-column grid, pivoted to horizontal carousel, then settled on vertical scroll with full-width cards
- Final design: Vintage ticket-style cards with flip animation
- Color palette changed from earth tones to vibrant tricolor: Pink (#C87E96) for missed, Teal (#C9414E) for done, Chartreuse (#D6D385) for empty

**Current Status:**
- Created FlipCardView.swift with 3D rotation animation
- Implemented ticket shape with perforated edges
- Front shows date + status (TODAY/MISSED/DONE stamp)
- Back shows keyword in coffee label style with word count, likes, comments stats
- Vertical scroll through 31 days (past 30 + today)

**Files Modified:**
- DailyPromptView.swift - Card grid layout
- FlipCardView.swift - Ticket-style flip card component
- KeywordsService.swift - Added getKeywordForDate() helper

**Next Steps:**
- Test build and fix any remaining compile errors
- Move streak/dot calendar to ProfileView
- Polish animations and interactions


---

## Later Session - Monthly Card Deck with Coffee Label Aesthetic

**Palette Update:**
User provided a new sophisticated color palette from Jenny Henderson Studio:
- Navy (#374375) - Primary dark
- Cloud (#FFFCF5) - Off-white/cream
- Periwinkle (#BABDE2) - Soft blue-lavender  
- Maroon (#895159) - Deep reddish-brown
- Peach (#DFAAA1) - Soft pink-beige

**Design Pivot:**
Pivoted to coffee label aesthetic inspired by coffee packaging - clean, elegant typography with:
- Thin horizontal dividers (0.5pt lines)
- Grid layout for details (MONTH / STATUS / WRITING)
- Large day numbers with small uppercase date labels
- Circular status stamps (checkmark, TODAY, LOCKED)
- Sharp 4pt corners (more refined than rounded cards)
- Subtle shadows for depth

**Architecture Changes:**
- Complete rewrite of DailyPromptView.swift
- Monthly deck with horizontal TabView for month swiping
- Vertical ScrollView for days 1-31 within each month
- Future days locked (not flippable, keyword secret)
- Auto-scroll to today's card on load
- Back card shows keyword in quotes with write button or stats

**Color Mapping Updated:**
- Navy (#374375) - Done/Written cards + accents
- Maroon (#895159) - Missed/Past empty cards
- Cloud (#FFFCF5) - Empty/Future cards + background + back side

**Fixes Applied:**
- Fixed duplicate Color.init(hex:) extension in FlipCardView.swift
- Added getKeywordForDate(date:language:) to KeywordsService.swift
- Updated DailyPromptView to use KeywordsService instead of KeywordGenerator for per-date keywords

**Time:** 04:44 KST (April 21)

## DailyWrite Color Palette Update (05:32)

Updated card color scheme:
- **Burnt Nectar** (#C2441C) - Future cards
- **Moss Shadow** (#AA542B) - Done/completed cards
- **Ruby Leaf** (#852E47) - Missed days
- **Cream** (#F5F0E8) - Today's card (empty/active state)

Added approved stamp design with stars around circle edge and diagonal stripes for DONE status.

Fixed Shape modifier chain - `.fill()` must come before `.frame()` when using custom Shape.

Added `onEssayTap` closure to MonthCardView for navigating to essay detail when tapping back of written cards.

---

## Memory Flush - April 21, 2026 06:49 AM

### DailyWrite Keyword Stability Fix

**Problem:** Keywords were changing position every time the app launched because `String.hashValue` is not stable across app launches.

**Solution:** Replaced unstable hash with stable date-based calculation:
```swift
// Old (unstable):
let poolIndex = abs(dateString.hashValue) % 100

// New (stable):
let components = calendar.dateComponents([.year, .month, .day], from: date)
let poolIndex = ((components.year! * 10000) + (components.month! * 100) + components.day!) % 100
```

**Files Modified:** DailyPromptView.swift - `generateMonthCards()` function

### DailyWrite Card Back Design

User preference confirmed: Back of cards should have **cream background** with **colored text** matching the front card color (not cream text on colored background).

This creates a nice visual effect where the card's state color (Indigo/Moss/Ruby/Burnt) carries through to the back via the text color.

---

## DailyWrite Debugging Session - Card Color Not Updating (09:49)

**Problem:** After writing an essay, the card color doesn't change to Moss Shadow (green/brown "done" state). It stays blue (Indigo Rain) or shows orange (Burnt Nectar).

**Investigation:**
1. Checked `cardBackground` computed property - order was wrong: `isFuture` was checked before `isWritten`
2. Fixed priority: `isWritten` now checked first, then `isFuture`, then `!isToday`, then default

**Root Cause Found:**
The `isWritten` calculation uses `essays[card.keyword]` dictionary lookup. Debug logs showed:
- 27 essays loaded from Firestore
- Multiple essays for same keyword (e.g., 4 essays for "지혜")
- Dictionary only keeps last essay per keyword
- Some essays have `deletedAt` set (soft deleted)
- If the last essay for a keyword is deleted, `isWritten` returns false even if non-deleted essays exist

**Fix Applied:**
Modified `loadEssaysForMonth()` to prefer non-deleted essays when building dictionary:
```swift
// Only add if no existing essay OR existing is deleted and this one isn't
if existing == nil || (existing?.deletedAt != nil && essay.deletedAt == nil) {
    essayDict[keyword] = essay
}
```

**Additional Issues Found:**
1. Delete functionality not working from essay detail view
2. User reported: "Already has essay" blocking access to writing even after deletion

**Status:** Ongoing debugging. User needs to test if card color updates after refresh.

---

## DailyWrite Delete Function Investigation (10:06)

**Problem:** Delete button in essay detail view (KeywordArchiveView.swift) not working. User taps "..." menu → Delete → nothing happens.

**Console Output:**
```
Called -[UIContextMenuInteraction updateVisibleMenuWithBlock:] while no context menu is visible. This won't do anything.
```

**Analysis:**
- The UI warning suggests the context menu (the "..." button menu) isn't showing properly
- The delete function `softDeleteEssay()` exists and looks correct
- The alert shows with "Move to Recently Deleted?" message
- But the actual delete doesn't seem to execute

**Next Steps:**
1. Verify the Menu button is working (the "..." ellipsis in top right)
2. Check if the alert confirmation is being tapped
3. Verify `deleteEssay()` async function is being called
4. Check Firebase console for actual delete operations

**Files Involved:**
- KeywordArchiveView.swift (essay detail view with delete button)
- FirebaseService.swift (softDeleteEssay function)
- DailyPromptView.swift (card color logic, dictionary building)

---



---

## DailyWrite Tab Bar Redesign & StreaksView (10:30-10:53)

**New 5-Tab Layout:**
1. **Home** - Card view (DailyPromptView with monthly flip cards)
2. **Streaks** - Monthly dots calendar showing writing activity
3. **+** (Center) - Prominent circular button for writing today's essay
4. **Feed** - Existing feed view
5. **Profile** - Existing profile view

**Custom Tab Bar Implementation:**
- Replaced standard TabView with ZStack overlay containing custom tab bar
- Center + button elevated with shadow (56pt circle, Indigo Rain background)
- Tab bar background: Cream (#F5F0E8) with subtle top border
- Icons: house, flame.fill, plus, newspaper, person

**StreaksView Features:**
- Streak badge with flame icon (Burnt Nectar color)
- Monthly dots calendar (30-day grid with rounded rectangles)
- Stats: Words Written, Published count
- Color scheme matching card states:
  - Moss Shadow (#4A5A30) - Written days
  - Indigo Rain (#0D244D) - Today (if not written)
  - Ruby Leaf (#852E47) - Missed days
  - Burnt Nectar (#C2441C) - Future days
  - Yellow ring - Around today if not yet written

**Files Modified:**
- ContentView.swift - Complete MainTabView rewrite with custom tab bar
- Added StreaksView, MonthlyStreakView, StreakDayCell
- Added WritingSheetView for center + button

**Technical Notes:**
- Center button opens WritingSheet with today's keyword pre-loaded
- Uses NotificationCenter to trigger reload when essay deleted
- StreaksView uses original MonthlyStreakView code (rounded rectangles, not dots)
- Color priority matches card design: written = green, today empty = navy, missed = ruby, future = burnt

---

## DailyWrite Day of Week Display (10:54)

Added day of week abbreviation (MON, TUE, WED...) next to the big date number on cards.

**Implementation:**
- Added `dayOfWeekString(from:)` helper in MonthCardView
- Uses DateFormatter with "EEE" format for short weekday
- Placed next to day number in card header
- Uppercase for consistency with coffee label aesthetic

**Files Modified:**
- DailyPromptView.swift - Added dayOfWeekString function inside MonthCardView

---

## DailyWrite Tab Bar & Theme Cleanup (11:15-11:25)

### Tab Bar Layout Fix
**Problem:** Content at bottom of screen was being cut off awkwardly by custom tab bar.

**Solution:**
- Removed floating effect from center + button (now inline with other tabs)
- Tab bar extends fully to bottom edge with `ignoresSafeArea(edges: .bottom)`
- Added 100pt bottom padding to content views so they don't get hidden
- Tab bar background: Cream (#F5F0E8) with subtle top border line

**Files Modified:**
- ContentView.swift - Updated customTabBar to remove floating, extend to bottom
- DailyPromptView.swift - Added `.padding(.bottom, 100)` to monthlyCardDeckView
- FeedView.swift - Added bottom padding
- ProfileView.swift - Added bottom padding

### Theme Simplification
**Changes:**
- Fixed accent color to **Indigo Rain** (#0D244D) - no more switching
- Removed light/dark theme toggle - app now always uses light theme
- Deleted `ThemePickerView.swift` entirely
- Removed `AccentColorPickerView` from `SettingsView.swift`
- Simplified `ThemeManager.swift` to only support light theme + Indigo Rain accent
- Removed theme picker references from Xcode project file

**Files Modified:**
- ThemeManager.swift - Simplified to fixed light theme + Indigo Rain accent
- DailyWriteApp.swift - Fixed `.tint()` and `.accentColor()` to Indigo Rain
- ContentView.swift - Removed `preferredColorScheme` binding
- SettingsView.swift - Removed theme and accent color pickers from UI
- Deleted ThemePickerView.swift

**Result:** Cleaner, simpler app with consistent coffee label aesthetic throughout.

---

## DailyWrite ProfileView Redesign (11:35-11:42)

### ProfileView Redesign
Redesigned ProfileView with coffee label aesthetic matching the rest of the app.

**Changes:**
- **Background:** Beige/cream (#F5F0E8) full-screen background
- **Menu buttons:** White cards with subtle shadow, Indigo Rain icons/text, 12pt rounded corners
- **Removed sections:** "내 글 전체보기" (Your Essays list) and "소개" (bio) - cleaner layout
- **Weekly statistics:** 7-day bar chart with Moss Shadow (#4A5A30) fill for active days, light Indigo background for empty days
- **Show all button:** Added "Show all" button in weekly stats header that navigates to WritingAnalyticsView
- **Header colors:** Updated ProfileHeader and ProfileStatView to use Indigo Rain (#0D244D) for all text and accents

**Files Modified:**
- ProfileView.swift - Complete redesign with beige background, white menu cards, weekly stats
- ProfileHeader - Changed `isFollowing` from `let` to `@Binding` to properly reflect follow state changes
- EditProfileView - Removed bio (소개) section from edit form

**Bug Fixes:**
- Fixed ProfileView.swift line 42 missing closing brace causing `monthHeader` scope error
- Fixed ProfileView.swift line 665 optional binding error with `calendar.date(byAdding:)` - changed `if let` to `guard let`
- Fixed ProfileView.swift line 198 - `essay.createdAt` is not optional, removed `guard let`

---

## DailyWrite KeywordArchiveView Redesign (11:40-11:42)

### Writing Archive Calendar Redesign
Completely redesigned the 글 보관함 (Writing Archive) with a clean calendar view inspired by event planning apps.

**Design Features:**
- **Cream background** (#F5F0E8) matching app aesthetic
- **Month header** with circular navigation buttons (chevrons in white circles)
- **Calendar card** - White rounded card with shadow containing:
  - Weekday headers (Su Mo Tu We Th Fr Sa)
  - 7-column grid with day numbers
  - **Status dots** below each day:
    - 🟢 Moss green - Written
    - 🔵 Indigo - Today (empty)
    - 🔴 Ruby - Missed
    - 🟠 Burnt - Future
- **Selected day card** - White card showing:
  - Date with day of week (e.g., "Apr 21, Monday")
  - Keyword for that day
  - Status badge (Written/Today/Missed/Upcoming)
  - Essay list or empty state with "Write now" button
- **Essay cards** - Clean list items with emoji, title, word count, likes

**Files Created:**
- EssayDetailView.swift - New view for displaying individual essays with header, content, stats, edit/delete options

**Files Modified:**
- KeywordArchiveView.swift - Complete rewrite with new calendar design
- DailyWrite.xcodeproj/project.pbxproj - Added EssayDetailView.swift to project

---



---

## DailyWrite EssayDetailView & Comments Redesign (12:00-12:09)

### EssayDetailView Redesign
Redesigned the essay detail view with cleaner aesthetic matching the app design.

**Changes:**
- **Page background:** Beige/cream (#F5F0E8) for the entire view
- **Header section:** Clean layout with keyword badge, title with indigo line separator (no box), author info
- **Content section:** Paper texture background (grid + speckles) with rounded corners, thin indigo border
- **Stats section:** White card with likes, comments count, word count, visibility icon

**Files Modified:**
- EssayDetailView.swift - Complete layout redesign with paper texture for content only

### Comments Inline Display
Changed from sheet-based comments to inline display in EssayDetailView.

**Changes:**
- Comments now show below the stats section without tapping
- Clean white card with "Comments" header and count
- Each comment shows author name, timestamp, and content
- Input field at bottom for adding new comments
- Removed CommentsView sheet - all inline now

**Files Modified:**
- EssayDetailView.swift - Added commentsSection, loadComments(), postComment()
- FeedView.swift - Added PaperTextureBackground struct (shared), updated CommentsView styling

### FeedView Simplification
Removed "Today" and "By Topic" tabs from FeedView.

**Changes:**
- Only 2 tabs now: "Following" and "Recent"
- Removed Today filter (case 1) and By Topic filter (case 3)
- Removed TopicSort enum, topicGroups state, selectedTopic state
- Removed getTopicGroups() and getEssaysForKeyword() functions
- Removed swipe gesture handlers
- Cleaned up swipe gesture handlers

**Files Modified:**
- FeedView.swift - Simplified to 2-tab layout

---

## DailyWrite Build Fixes (12:01-12:09)

### Duplicate Declarations Fixed
Fixed multiple compilation errors from code refactoring:

1. **EssayDetailView.swift line 19:** Duplicate `@State` declarations for `comments` and `newComment`
   - Removed duplicate declarations

2. **EssayDetailView.swift line 457:** Duplicate `PaperTextureBackground` struct
   - Removed from EssayDetailView.swift (kept in FeedView.swift as shared component)

3. **EssayDetailView.swift:** Removed unused `showingComments` @State variable
   - Comments now inline, no sheet needed

**Result:** Clean build with no redeclaration errors.

---

---

## DailyWrite Comments Threading Fix (12:20-12:27)

### Problem: 대댓글 (Nested Replies) Not Working
Comments were displaying but nested replies (대댓글) were not showing up.

**Root Cause:**
1. Optional String comparison issue: `parentCommentId` and `comment.id` are both optional, but direct comparison wasn't working
2. Reply posting wasn't implemented - the reply button just closed the field without posting

**Fixes Applied:**

1. **Fixed reply filtering** in commentsSection:
```swift
// Before (not working):
let replies = comments.filter { $0.parentCommentId == comment.id }

// After (working):
let replies = comments.filter { 
    if let replyParentId = $0.parentCommentId, 
       let commentId = comment.id {
        return replyParentId == commentId
    }
    return false
}
```

2. **Added onReply callback** to CommentRowView:
```swift
struct CommentRowView: View {
    let comment: Comment
    let replies: [Comment]
    let onReply: (String) -> Void  // Added callback
    // ...
}
```

3. **Implemented postReply function** in main view:
```swift
private func postReply(parentCommentId: String, content: String) {
    guard let essayId = essay.id, !content.isEmpty else { return }
    
    Task {
        do {
            try await FirebaseService.shared.addComment(
                essayId: essayId,
                content: content,
                parentCommentId: parentCommentId
            )
            await loadComments()  // Refresh to show new reply
        } catch {
            print("Error posting reply: \(error)")
        }
    }
}
```

4. **Reply button now posts** instead of just closing:
```swift
Button {
    onReply(replyText)  // Actually posts the reply
    showReplyField = false
    replyText = ""
} label: {
    Image(systemName: "arrow.up.circle.fill")
}
```

**Result:** Nested replies now display properly under their parent comments, and users can post new replies that persist to Firebase.

**Files Modified:**
- EssayDetailView.swift - Updated CommentRowView with onReply callback, added postReply function, fixed reply filtering

---
# Memory Log - 2026-04-21

## Session Summary

### FeedView Essay Card Redesign
- User shared reference image of clean paper card aesthetic (white paper with subtle shadow on beige background)
- EssayCard already updated with paper aesthetic matching the reference:
  - Paper color: #FDFBF7 (slightly brighter than page background #F5F0E8)
  - Subtle shadow on right/bottom (color: black opacity 0.08, radius: 12, offset: x:4 y:4)
  - Sharp corners (cornerRadius: 2) for clean paper look
  - Paper edge highlight (top/left) with white opacity 0.5
  - Indigo Rain (#0D244D) text throughout
  - Section dividers with subtle opacity

### EssayDetailView Improvements
- Fixed word count icon from non-existent `text.word.count` to `textformat` (SF Symbols)
- Stats section now shows consistent icons:
  - Heart for likes

  - Bubble for comments
  - Textformat for word count
  - Globe/Lock for visibility

### Font Preservation in EssayDetailView
- Added AttributedTextView (UIViewRepresentable wrapping UITextView) to properly display rich text formatting
- Maps AppFont rawValues to actual font file names (e.g., "KoPub Batang" → "KoPubBatang-Regular")
- Supports all AppFont cases with proper line spacing from essay settings
- Falls back to plain Text when no attributed content exists

### Current Status
- All major UI redesigns completed (DailyPromptView, FeedView, ProfileView, KeywordArchiveView, EssayDetailView)
- Comments threading (대댓글) implemented with proper parent-child relationships
- Paper texture with masking tape aesthetic applied to essay content
- Build ready for testing

---
**Last Updated:** 2026-04-21 12:42 PM (Asia/Seoul)


---

## DailyWrite FeedView Layout Fix (12:47)

### Problem: Essay Content Overflow
Essay content in EssayDetailView was spreading too wide beyond screen bounds.

### Solution Applied to AttributedTextView:
1. Added `widthTracksTextView = true` to text container
2. Set horizontal compression resistance to `.defaultLow`
3. Implemented `sizeThatFits` to calculate intrinsic size with width constraints (accounting for 56pt padding)

### Solution Applied to contentSection:
1. Added explicit `frame(maxWidth: .infinity)` to AttributedTextView and Text fallback
2. Added `alignment: .topLeading` to ZStack for proper anchoring

**Files Modified:** EssayDetailView.swift

---

## DailyWrite FeedView Build Fixes (12:47)

### Task Ambiguity Fix
Fixed "Use of 'Task' refers to instance method rather than generic struct 'Task' in module '_Concurrency'" errors:
- Changed all standalone `Task { }` to `_Concurrency.Task { }` to disambiguate
- `group.addTask` is part of TaskGroup API and doesn't need prefix

### Struct Scope Fix
Fixed "'private' can only be used in a non-local scope" errors:
- Helper functions (`navigateToEssay`, `loadEssays`, `enrichWithAuthors`, etc.) were inside the body due to missing closing braces
- Added proper closing braces after line 176 to close body and keep functions at struct level
- Removed orphaned Task block that was leftover from old EssayCard implementation

### Brace Balance
Verified all braces balanced (155 open, 155 close) - file now parses successfully.

**Files Modified:** FeedView.swift

---

**Last Updated:** 2026-04-21 12:57 PM (Asia/Seoul)

---

## DailyWrite - Responsive Layout & Essay Detail View Fixes (14:11-14:21)

### Responsive Layout Changes (Reverted)
Attempted to add responsive layout helpers to EssayDetailView.swift:
- Added GeometryReader with screen size detection
- Responsive padding calculations based on device type
- Reverted due to text overlap issues with line spacing

### Final State: Reverted to Simple Layout
- Removed GeometryReader wrapper
- Restored fixed padding values (28pt horizontal, 32pt vertical)
- Text displays correctly without overlap

### Missed Stamp Design Updates
Updated missed stamp in DailyPromptView.swift:
- Rectangular shape with rounded corners (2pt radius)
- Rotation: -22 degrees (diagonal placement)
- Double border removed, single solid border instead
- Position offset upward (-8pt y offset)
- Uses textColor (cream) matching other card text

### Weekday Display (요일 → MON/TUE/WED...)
Changed day of week display in DailyPromptView.swift:
- Now shows English abbreviations (MON, TUE, WED...) even in Korean version
- Uses `formatter.locale = Locale(identifier: "en_US")` to force English
- Font size increased to 16pt (was 10pt)
- Tracking increased to 2
- Full opacity (0.9 instead of 0.7)

### Removed Status Text from Card Headers
Removed "MISSED"/"COMPLETED" text from card headers in DailyPromptView.swift:
- Now only shows day number and weekday
- Status shown via stamp icon on right side
- Cleaner, more aesthetic design

**Files Modified:**
- EssayDetailView.swift - Reverted responsive layout, kept simple design
- DailyPromptView.swift - Missed stamp design, weekday in English, removed status text

---

## DailyWrite - Essay Editor Redesign (14:15-14:21)

### SimpleWritingEditorView Redesign
Applied mockup design from reference image to the essay editor:

**Top Bar:**
- Cancel button: White capsule shape with rounded corners
- Complete button: Dark green (#2d3f2f) when ready, gray when not
- Status-based color changes

**Today's Prompt Section:**
- Small uppercase label with tracking: "TODAY'S PROMPT"
- Large keyword display (32pt, bold)
- Centered alignment

**Title Field:**
- Centered text alignment
- Divider underneath (thin gray line)
- System font, semibold weight

**Progress Bar:**
- Color-coded based on progress:
  - Green (#4a7c59): Complete (≥300 chars)
  - Light green (#7a9e5a): ≥67% progress
  - Yellow (#c4a84a): ≥33% progress
  - Gray (#d1ccc4): Below 33%
- Shows "✓ 완료 가능" when complete, "current / 300자" otherwise

**Background:**
- Beige/cream color matching app aesthetic

**Files Modified:**
- SimpleWritingEditorView.swift - Complete UI redesign with progress bar, modern buttons

---

**Last Updated:** 2026-04-21 14:21 PM (Asia/Seoul)


---
**Session Update - 14:31 KST**

## SimpleWritingEditorView UI Updates

### Completed:
- ✅ Fixed duplicate Cancel buttons (removed navigation toolbar)
- ✅ Changed Complete button to open visibility bottom sheet
- ✅ Added tabbed bottom toolbar (서식/폰트/정렬) with modern styling
- ✅ Updated bottom sheet with vertical button layout
- ✅ Changed all key UI colors to default #0D244D:
  - Complete button background
  - Progress bar (300+ chars)
  - Visibility checkmark
  - Publish button

### Current State:
- Progress bar colors: gray (0-99), yellow/gold (100-199), light green (200-299), default blue (#0D244D) (300+)
- Bottom toolbar has 3 tabs: 서식 (formatting), 폰트 (font), 정렬 (alignment)
- Visibility bottom sheet shows 3 options with vertical action buttons


## DailyWrite SimpleWritingEditorView Fixes (15:00-15:20)

### Keyword Display Issue
**Problem:** Writing Editor를 열 때 "Today's Prompt" 라벨은 보이는데 키워드 텍스트가 안 보임 ("NO KEYWORD"로 표시됨)

**Root Cause:** 
- ContentView와 DailyPromptView 모두 `.fullScreenCover`로 Writing Editor를 염
- 하지만 `dailyKeyword`가 아직 로드되지 않은 상태에서 에디터가 열림
- `onAppear`에서 `loadTodayKeyword()`를 호출하지만, 사용자가 탭을 누르는 타이밍에 아직 실행 중일 수 있음

**Fix Applied:**
- ContentView.swift line 135: `SimpleWritingEditorView(keyword: dailyKeyword.isEmpty ? "오늘" : dailyKeyword)`
- DailyPromptView.swift line 47: `SimpleWritingEditorView(keyword: dailyKeyword.isEmpty ? "오늘" : dailyKeyword)`

이렇게 하면 키워드가 비어있을 때 "오늘"이라는 fallback 키워드가 표시됩니다.

### Complete Button Localization
**Problem:** "Complete" 버튼이 영어로 표시됨 (한국어로 되어야 함)

**Fix Applied:**
- en.lproj/Localizable.strings: `"Complete" = "Complete";` 추가
- ko.lproj/Localizable.strings: `"Complete" = "완료";` 추가

### Writing Area Width
**Problem:** Writing area가 좁게 보임

**Fix Applied:**
- Title TextField 패딩: `.padding(.horizontal, 24)` → `.padding(.horizontal, 20)`
- RichTextEditor 패딩: `.padding(.horizontal, 24)` → `.padding(.horizontal, 20)`

### Full-Screen Cover & Visibility Sheet
**Changes:**
- Writing Editor presentation: `.sheet` → `.fullScreenCover`
- Visibility selector: `.sheet` → `.fullScreenCover` with NavigationStack and close button (X)
- Removed `.presentationDetents` and `.presentationDragIndicator` (not compatible with fullScreenCover)

### Bottom Toolbar Tabs
**Implemented:**
- 서식 (Bold/Italic/Underline/Strikethrough)
- 폰트 (Font family dropdown + Size dropdown)
- 정렬 (Left/Center/Right/Justify)

**Font Dropdown:**
- Uses `Menu` component with `ForEach(AppFont.allCases)`
- System font vs custom font handling with `.otf` extension
- Size dropdown: 11pt-24pt range

### Debug Changes (Reverted After Testing)
- Added yellow background to keyword area for debugging
- Added "NO KEYWORD" fallback text when keyword is empty
- Changed keyword text color to black for visibility testing
- These should be removed in production build

**Files Modified:**
- SimpleWritingEditorView.swift
- ContentView.swift
- DailyPromptView.swift
- Localizable.strings (en, ko)

## 2026-04-21 Session Summary

### Completed Tasks

#### EssayDetailView Fixes
- Fixed scope errors for `isCurrentUserAuthor`, `deleteEssay`, and `postReply`
- Fixed brace balance issues with nested AttributedTextView struct
- Fixed custom font loading by adding .otf extension to font names
- Switched to native SwiftUI `Text(attributedContent)` for rich text display
- Reverted responsive layout due to text overlap issues, restored simple fixed padding

#### DailyPromptView Updates
- Added rectangular "MISSED" stamp with -22° rotation and cream text color
- Removed status text from card headers (now shown only via stamp)
- Updated weekday display to English abbreviations (MON, TUE, etc.) using en_US locale
- Increased weekday font size from 10pt to 16pt

#### SimpleWritingEditorView Redesign
- Converted from .sheet to .fullScreenCover presentation
- Redesigned bottom toolbar with tabbed interface (서식/폰트/정렬)
- Implemented new unified toolbar design with:
  - Font dropdown (글꼴)
  - Size dropdown (14pt)
  - Formatting buttons (B/I/U)
  - Alignment buttons (left/center/right icons)
  - Capsule-shaped container (#f2f2f2 background) with shadow
- Removed duplicate Cancel buttons
- Complete button now opens visibility bottom sheet
- Updated progress bar color logic to word count intervals (Ruby Leaf → Burnt Nectar → Moss Shadow → Indigo Rain)
- Made Complete button always enabled (removed 300 character minimum restriction)
- Fixed button tap responsiveness with zIndex and allowsHitTesting

#### Keyword Display Fix
- Fixed @State race condition causing keyword not to show in Writing Editor
- Changed ContentView to compute keyword inline in fullScreenCover: `todayKeyword.isEmpty ? computeTodayKeyword() : todayKeyword`
- Unified keyword calculation across all views using stable hash algorithm in KeywordsService
- Set keyword text color to Ruby Leaf (#852E47)

#### Localization
- Added "Complete" = "완료" to ko.lproj/Localizable.strings
- Added "Complete" = "Complete" to en.lproj/Localizable.strings

### Technical Learnings
- @State updates are batched in SwiftUI; presenting views immediately after state changes risks using stale values
- String does not conform to Identifiable, preventing use of fullScreenCover(item:)
- Custom fonts (.otf) require explicit file extension in .custom() initializer
- SwiftUI native Text(AttributedString) provides more reliable sizing than UIViewRepresentable
- Button gesture handling requires zIndex and allowsHitTesting for proper hit testing

### Next Steps
- Test new unified toolbar design
- Verify keyword display works correctly from all entry points
- Complete button localization testing

## 16:34 - RichTextEditor Brace Balance Fix

Fixed duplicate code in RichTextEditor.swift that was causing compilation errors.
- Removed duplicate formatting methods (toggleAttribute, applyAlignment)
- Fixed brace balance issues in Coordinator class
- Verified all methods (applyBold, applyItalic, etc.) are properly inside Coordinator class
- Build successful

## Session Summary (16:00-16:30)

### Completed Tasks:
1. **RichTextEditor Toolbar** - Moved to inputAccessoryView, matches Apple Notes style with B/I/U buttons, font dropdown, size dropdown, alignment buttons
2. **Editor UX Improvements**:
   - Toolbar appears above keyboard and moves with it
   - Tap gesture on screen dismisses keyboard (while preserving text editing)
   - Text buttons use Indigo Rain color
3. **SimpleWritingEditorView** - "Complete" button hardcoded to "완료" (localization issue)
4. **DailyPromptView** - Scrolls to today's card on load (center anchor)

### Key Technical Decisions:
- Using UIGestureRecognizerDelegate with shouldRecognizeSimultaneouslyWith to allow both tap and text editing gestures
- Coordinator class properly organized with all formatting methods inside

### Next Steps (if needed):
- Test keyboard dismiss gesture on actual device
- Verify today's card scroll position works reliably after data loads

## 17:19 - Firebase Cloud Functions 배포 완료

Firebase Functions 배포 완료. 푸시 알림 시스템 활성화됨.

**배포된 Functions:**
- `sendPushNotification` - pushNotifications 컬렉션 감시 → FCM 전송
- `onCommentCreated` - 댓글 작성 시 알림 생성
- `onLikeCreated` - 좋아요 시 알림 생성  
- `onFriendRequestCreated` - 친구 요청 시 알림 생성

**NotificationsView 수정:** 인앱 알림 탭 시 타입별 이동 구현
- Follow → PublicProfileView
- Friend Request → FriendsView
- Comment/Reply/Like → EssayDetailView (sheet)

## 18:15 - 댓글 작성 버그 수정

**문제:** 새로 생성된 Essay에서 댓글이 작성되지 않음

**원인:** 
- SimpleWritingEditorView에서 essay 저장 후 dismiss만 하고, 저장된 essay 정보를 DailyPromptView로 전달하지 않음
- DailyPromptView의 `selectedEssay`가 설정되지 않아 EssayDetailView가 열리지 않음
- 결과적으로 dismiss 후에는 Feed 탭에서 essay를 찾아야 하므로, 새 essay view로 바로 이동하지 않음

**해결:**
- SimpleWritingEditorView: 새 essay 생성 시 `essayCreated` Notification 발송 (userInfo에 essayId 포함)
- DailyPromptView: Notification 수신하여 `selectedEssay` 설정 후 EssayDetailView 자동 표시

**변경된 파일:**
- `/Users/leia/.openclaw/workspace/DailyWrite/DailyWrite/Views/SimpleWritingEditorView.swift`
- `/Users/leia/.openclaw/workspace/DailyWrite/DailyWrite/Views/DailyPromptView.swift`

## 20:50 - UI Polish Session

Completed several UI refinements:

1. **FlipCardView**: Changed rotation direction to clockwise (y-axis rotation angle sign flipped)

2. **Button Borders**:
   - "작성하기" (FlipCardView back): Added white border
   - "보기" (DailyPromptView front): Changed border to cream color `#F5F0E8`
   - "둘러보기": Border now matches text color (`cardBackground`)

3. **KeywordBrowseView**: Reverted to show only public essays (`visibility == .public`)

All buttons now have consistent borders and visual weight across the app.
## 21:56 - FlipCardView & DailyPromptView 디자인 개선

**완료된 작업:**

1. **FlipCardView 뒷면 디자인 변경**
   - "작성하기"/"오늘의 글쓰기" 버튼 제거
   - 디바이더 + 하단 전체 터치 영역으로 변경
   - 반투명 흰색 배경 (`Color.white.opacity(0.15)`)
   - 텍스트만 표시 (15pt semibold)

2. **DailyPromptView 뒷면 디자인 개선**
   - "PROMPT", "DAY XX" 헤더 제거
   - "보기" 버튼 네모 모양 제거, 텍스트만 표시
   - 세로 디바이더 추가 (height: 28)
   - 가로 디바이더 위치 조정 (padding.top: 10)
   - 세로 디바이더 위치 조정 (padding.top: 6)

**최종 카드 디자인 패턴:**
```
┌─────────────────────────────┐
│      (앞면: 날짜/테마)       │
├─────────────────────────────┤
│      "키워드" (뒷면)         │
│                             │
│            ...              │
│                             │
│ ─────────────────────────── │  ← 가로 디바이더
│                             │
│   작성하기  │  둘러보기     │  ← 텍스트 + 세로 디바이더
│   (보기)    │               │
└─────────────────────────────┘
```

**파일 변경:**
- `/DailyWrite/DailyWrite/Views/FlipCardView.swift`
- `/DailyWrite/DailyWrite/Views/DailyPromptView.swift`

---
## DailyWrite - Compiler Error Fix (22:34)

### Problem
Compiler error: `Extraneous argument label 'hex:' in call` at FlipCardView.swift:41

### Root Cause
The `Color(hex:)` extension was not registered in the Xcode project file. The file was created at `DailyWrite/Utils/ColorExtensions.swift` but the project.pbxproj had an incorrect path reference.

### Fix Applied
1. Created `ColorExtensions.swift` in `DailyWrite/Utils/` with `Color(hex:)` initializer
2. Updated project.pbxproj to correct the file reference path:
   - Added proper PBXBuildFile entry
   - Fixed PBXFileReference to use `name = ColorExtensions.swift; path = DailyWrite/Utils/ColorExtensions.swift`
   - Added to PBXGroup children list
   - Added to PBXSourcesBuildPhase

### Color Extension Implementation
```swift
extension Color {
    init(hex: String) {
        let hex = hex.trimmingCharacters(in: CharacterSet.alphanumerics.inverted)
        var int: UInt64 = 0
        Scanner(string: hex).scanHexInt64(&int)
        let a, r, g, b: UInt64
        switch hex.count {
        case 3: // RGB (12-bit)
            (a, r, g, b) = (255, (int >> 8) * 17, (int >> 4 & 0xF) * 17, (int & 0xF) * 17)
        case 6: // RGB (24-bit)
            (a, r, g, b) = (255, int >> 16, int >> 8 & 0xFF, int & 0xFF)
        case 8: // ARGB (32-bit)
            (a, r, g, b) = (int >> 24, int >> 16 & 0xFF, int >> 8 & 0xFF, int & 0xFF)
        default:
            (a, r, g, b) = (1, 1, 1, 0)
        }
        
        self.init(
            .sRGB,
            red: Double(r) / 255,
            green: Double(g) / 255,
            blue: Double(b) / 255,
            opacity: Double(a) / 255
        )
    }
}
```

**Files Modified:**
- DailyWrite/Utils/ColorExtensions.swift (created)
- DailyWrite.xcodeproj/project.pbxproj (updated file references)

**Time:** 22:41 KST (April 21, 2026)

---
## DailyWrite - Compiler Fixes Session (22:59 - 23:01)

### Fix 1: `getEssaysForMonth` Method
**Error:** `Value of type 'FirebaseService' has no member 'getEssaysForMonth'`
**Fix:** Added `getEssaysForMonth(userId:date:)` method to FirebaseService.swift

```swift
// MARK: - Month Query
func getEssaysForMonth(userId: String, date: Date) async throws -> [Essay] {
    let calendar = Calendar.current
    
    // Get first day of month
    guard let startOfMonth = calendar.date(from: calendar.dateComponents([.year, .month], from: date)) else {
        return []
    }
    
    // Get first day of next month
    guard let startOfNextMonth = calendar.date(byAdding: .month, value: 1, to: startOfMonth) else {
        return []
    }
    
    // Use the existing date range query
    return try await getUserEssaysInDateRange(userId: userId, startDate: startOfMonth, endDate: startOfNextMonth)
}
```

### Fix 2: `likes` Property Name
**Error:** `Value of type 'Essay' has no member 'likes'`
**Fix:** Changed `essay.likes` to `essay.likesCount` in KeywordBrowseView.swift

The Essay model uses `likesCount` not `likes` for storing like counts.

### Fix 3: `KeywordBrowseView` Creation
**Error:** `Cannot find 'KeywordBrowseView' in scope`
**Fix:** Created new `KeywordBrowseView.swift` file in Views folder with:
- Full navigation stack with dismiss capability
- Essay list for a specific keyword
- EssayCard component with preview, author, likes, and word count
- Empty state when no essays exist
- Added to Xcode project.pbxproj

**Files Modified:**
- DailyWrite/Services/FirebaseService.swift (added getEssaysForMonth)
- DailyWrite/Views/KeywordBrowseView.swift (created, fixed likes → likesCount)
- DailyWrite.xcodeproj/project.pbxproj (added KeywordBrowseView.swift)

**Time:** 23:01 KST (April 21, 2026)
